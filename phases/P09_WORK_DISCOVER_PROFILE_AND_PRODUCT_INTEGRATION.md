# P09 工作、发现、我的与全产品体验整合

> 本阶段包含 **25 个 Feature ID**。本文件与 `contracts/feature-map.yaml` 一致；表中任何条目都不能仅以页面占位、静态返回或聊天声明视为完成。

## 1. 阶段目标

完成四栏信息架构、动态发现、作品和任务聚合、个人设置、通知、数据管理、更新和服务状态。

## 2. 阶段边界

- 只实现本文件列出的 Feature ID；下一阶段内容不得为了“顺手”提前改变数据契约。
- 每个可见功能必须同时完成必要的 Android/网页状态、后端接口或内部事件、持久化、管理后台控制、权限、日志指标和测试。
- 外部凭据缺失时完成 Adapter、契约 mock、feature flag 和 Owner Actions；仅真实联调条目标记 `BLOCKED_EXTERNAL`。
- 不使用永久假数据、固定余额、固定模型列表、静态成功响应或不可追踪占位作品。
- 阶段发布前，所有本阶段 Feature 状态必须是 `IMPLEMENTED`、`DEFERRED_WITH_REASON` 或 `BLOCKED_EXTERNAL`。

## 3. 建议的纵向实施顺序

1. **Admin 纵向切片**：`P09-011`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
2. **Android 纵向切片**：`P09-001`, `P09-002`, `P09-003`, `P09-004`, `P09-005`, `P09-006`, `P09-007`, `P09-008`, `P09-009`, `P09-010`, `P09-012`, `P09-013`, `P09-014`, `P09-015`, `P09-016`, `P09-017`, `P09-018`, `P09-019`, `P09-020`, `P09-021`, `P09-022`, `P09-023`, `P09-024`, `P09-025`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。

## 4. Feature 逐项实施合同

### P09-001 — 工作页聚合工具、项目、作品和任务

- **产品表面/页面或菜单**：`Android` / `工作`。
- **用户或管理员动作**：工作页聚合工具、项目、作品和任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/work`；负责服务为 `Mobile BFF`。
- **数据实体**：`projects|artifacts|jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/工作页配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“工作”完成“工作页聚合工具、项目、作品和任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/工作页配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-001-UNIT|P09-001-API|P09-001-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-002 — 按工具分类展示 AI 能力入口

- **产品表面/页面或菜单**：`Android` / `工作/工具`。
- **用户或管理员动作**：按工具分类展示 AI 能力入口。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/tools`；负责服务为 `Tool Configuration Service`。
- **数据实体**：`tool_configs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`AI工具/工具目录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“工作/工具”完成“按工具分类展示 AI 能力入口”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“AI工具/工具目录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-002-UNIT|P09-002-API|P09-002-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-003 — 按类型和时间展示作品库

- **产品表面/页面或菜单**：`Android` / `工作/作品`。
- **用户或管理员动作**：按类型和时间展示作品库。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/artifacts`；负责服务为 `Artifact Service`。
- **数据实体**：`artifacts|artifact_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`作品中心/作品列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“工作/作品”完成“按类型和时间展示作品库”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“作品中心/作品列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-003-UNIT|P09-003-API|P09-003-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-004 — 收藏、重命名、移动和删除作品

- **产品表面/页面或菜单**：`Android` / `作品菜单`。
- **用户或管理员动作**：收藏、重命名、移动和删除作品。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PATCH` `/api/mobile/v1/artifacts/{artifactId}`；负责服务为 `Artifact Service`。
- **数据实体**：`artifacts`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`作品中心/作品详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“作品菜单”完成“收藏、重命名、移动和删除作品”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“作品中心/作品详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-004-UNIT|P09-004-API|P09-004-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-005 — 统一展示全部异步任务

- **产品表面/页面或菜单**：`Android` / `工作/任务`。
- **用户或管理员动作**：统一展示全部异步任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/jobs`；负责服务为 `Job Service`。
- **数据实体**：`jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/任务队列`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“工作/任务”完成“统一展示全部异步任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/任务队列”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-005-UNIT|P09-005-API|P09-005-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-006 — 加载后台动态配置的发现页

- **产品表面/页面或菜单**：`Android` / `发现`。
- **用户或管理员动作**：加载后台动态配置的发现页。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/discover`；负责服务为 `CMS Service`。
- **数据实体**：`discover_cards|discover_categories`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/发现页`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“发现”完成“加载后台动态配置的发现页”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/发现页”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-006-UNIT|P09-006-API|P09-006-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-007 — 按分类筛选发现服务

- **产品表面/页面或菜单**：`Android` / `发现/分类`。
- **用户或管理员动作**：按分类筛选发现服务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/discover/categories/{categoryId}`；负责服务为 `CMS Service`。
- **数据实体**：`discover_cards`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/发现页`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“发现/分类”完成“按分类筛选发现服务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/发现页”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-007-UNIT|P09-007-API|P09-007-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-008 — 展示 Prompt 模板并带变量启动任务

- **产品表面/页面或菜单**：`Android` / `发现/Prompt模板`。
- **用户或管理员动作**：展示 Prompt 模板并带变量启动任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/prompt-templates`；负责服务为 `Template Service`。
- **数据实体**：`prompt_templates`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/Prompt模板`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“发现/Prompt模板”完成“展示 Prompt 模板并带变量启动任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/Prompt模板”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-008-UNIT|P09-008-API|P09-008-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-009 — 展示模型实验室和能力说明

- **产品表面/页面或菜单**：`Android` / `发现/模型实验室`。
- **用户或管理员动作**：展示模型实验室和能力说明。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/model-lab`；负责服务为 `Model Catalog Service`。
- **数据实体**：`models|model_capabilities`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/模型展示`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“发现/模型实验室”完成“展示模型实验室和能力说明”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/模型展示”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-009-UNIT|P09-009-API|P09-009-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-010 — 展示公告、活动和即将上线能力

- **产品表面/页面或菜单**：`Android` / `发现/公告`。
- **用户或管理员动作**：展示公告、活动和即将上线能力。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/announcements`；负责服务为 `CMS Service`。
- **数据实体**：`announcements|feature_flags`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/公告`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“发现/公告”完成“展示公告、活动和即将上线能力”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/公告”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-010-UNIT|P09-010-API|P09-010-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-011 — 后台创建、排序、灰度和下架发现卡片

- **产品表面/页面或菜单**：`Admin` / `内容运营/发现页`。
- **用户或管理员动作**：后台创建、排序、灰度和下架发现卡片。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET|POST` `/admin/v1/discover/cards`；负责服务为 `CMS Service`。
- **数据实体**：`discover_cards|feature_flags`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/发现页`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“内容运营/发现页”完成“后台创建、排序、灰度和下架发现卡片”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/发现页”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-011-UNIT|P09-011-API|P09-011-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-012 — 展示个人资料卡、头像、用户名和 UID

- **产品表面/页面或菜单**：`Android` / `我的`。
- **用户或管理员动作**：展示个人资料卡、头像、用户名和 UID。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/account/profile`；负责服务为 `User Service`。
- **数据实体**：`users|user_profiles`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`认证与用户/用户详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的”完成“展示个人资料卡、头像、用户名和 UID”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“认证与用户/用户详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-012-UNIT|P09-012-API|P09-012-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-013 — 编辑头像和用户名

- **产品表面/页面或菜单**：`Android` / `我的/个人资料`。
- **用户或管理员动作**：编辑头像和用户名。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PATCH` `/api/mobile/v1/account/profile`；负责服务为 `User Service`。
- **数据实体**：`user_profiles`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`认证与用户/用户详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/个人资料”完成“编辑头像和用户名”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“认证与用户/用户详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-013-UNIT|P09-013-API|P09-013-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-014 — 快捷进入我的项目、文件和作品

- **产品表面/页面或菜单**：`Android` / `我的`。
- **用户或管理员动作**：快捷进入我的项目、文件和作品。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/account/content-summary`；负责服务为 `Mobile BFF`。
- **数据实体**：`projects|attachments|artifacts`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`—`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的”完成“快捷进入我的项目、文件和作品”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态。
- **自动测试 ID**：`P09-014-UNIT|P09-014-API|P09-014-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-015 — 查看和管理收藏

- **产品表面/页面或菜单**：`Android` / `我的/收藏`。
- **用户或管理员动作**：查看和管理收藏。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/favorites`；负责服务为 `Favorite Service`。
- **数据实体**：`favorites`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/收藏统计`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/收藏”完成“查看和管理收藏”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/收藏统计”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-015-UNIT|P09-015-API|P09-015-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-016 — 设置默认模型、推理和回答风格

- **产品表面/页面或菜单**：`Android` / `我的/AI设置`。
- **用户或管理员动作**：设置默认模型、推理和回答风格。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/mobile/v1/preferences/ai`；负责服务为 `Preference Service`。
- **数据实体**：`user_preferences`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/默认配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/AI设置”完成“设置默认模型、推理和回答风格”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/默认配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-016-UNIT|P09-016-API|P09-016-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-017 — 设置全局自定义指令

- **产品表面/页面或菜单**：`Android` / `我的/自定义指令`。
- **用户或管理员动作**：设置全局自定义指令。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/mobile/v1/preferences/instructions`；负责服务为 `Memory Service`。
- **数据实体**：`user_instructions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/指令策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/自定义指令”完成“设置全局自定义指令”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/指令策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-017-UNIT|P09-017-API|P09-017-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-018 — 开启、查看和删除个人记忆

- **产品表面/页面或菜单**：`Android` / `我的/个人记忆`。
- **用户或管理员动作**：开启、查看和删除个人记忆。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET|DELETE` `/api/mobile/v1/memories`；负责服务为 `Memory Service`。
- **数据实体**：`memories`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/记忆管理`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/个人记忆”完成“开启、查看和删除个人记忆”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/记忆管理”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-018-UNIT|P09-018-API|P09-018-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-019 — 管理任务完成和安全通知

- **产品表面/页面或菜单**：`Android` / `我的/通知设置`。
- **用户或管理员动作**：管理任务完成和安全通知。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/mobile/v1/preferences/notifications`；负责服务为 `Notification Service`。
- **数据实体**：`notification_preferences`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`通知中心/用户设置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/通知设置”完成“管理任务完成和安全通知”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“通知中心/用户设置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-019-UNIT|P09-019-API|P09-019-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-020 — 设置主题、字体和语言

- **产品表面/页面或菜单**：`Android` / `我的/外观与语言`。
- **用户或管理员动作**：设置主题、字体和语言。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/mobile/v1/preferences/display`；负责服务为 `Preference Service`。
- **数据实体**：`user_preferences`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统设置/默认外观`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/外观与语言”完成“设置主题、字体和语言”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统设置/默认外观”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-020-UNIT|P09-020-API|P09-020-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-021 — 创建用户数据导出任务

- **产品表面/页面或菜单**：`Android` / `我的/数据与隐私`。
- **用户或管理员动作**：创建用户数据导出任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/privacy/exports`；负责服务为 `Privacy Service`。
- **数据实体**：`export_jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`安全与审计/数据导出`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`privacy_export`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/数据与隐私”完成“创建用户数据导出任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“安全与审计/数据导出”能够查询或控制该能力，变更写入审计日志；异步任务 privacy_export 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P09-021-UNIT|P09-021-API|P09-021-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-022 — 申请清空历史或注销账号

- **产品表面/页面或菜单**：`Android` / `我的/数据与隐私`。
- **用户或管理员动作**：申请清空历史或注销账号。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/privacy/deletion-requests`；负责服务为 `Privacy Service`。
- **数据实体**：`deletion_requests`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`安全与审计/删除请求`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`privacy_delete`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/数据与隐私”完成“申请清空历史或注销账号”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“安全与审计/删除请求”能够查询或控制该能力，变更写入审计日志；异步任务 privacy_delete 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P09-022-UNIT|P09-022-API|P09-022-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-023 — 检查 APK 更新和强制升级策略

- **产品表面/页面或菜单**：`Android` / `我的/检查更新`。
- **用户或管理员动作**：检查 APK 更新和强制升级策略。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/app/releases/latest`；负责服务为 `Release Service`。
- **数据实体**：`app_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统运营/App版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/检查更新”完成“检查 APK 更新和强制升级策略”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统运营/App版本”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-023-UNIT|P09-023-API|P09-023-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-024 — 提交帮助反馈、截图和诊断信息

- **产品表面/页面或菜单**：`Android` / `我的/帮助与反馈`。
- **用户或管理员动作**：提交帮助反馈、截图和诊断信息。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/support/tickets`；负责服务为 `Support Service`。
- **数据实体**：`support_tickets|attachments`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统运营/反馈工单`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/帮助与反馈”完成“提交帮助反馈、截图和诊断信息”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统运营/反馈工单”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-024-UNIT|P09-024-API|P09-024-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P09-025 — 展示服务状态和已知事故

- **产品表面/页面或菜单**：`Android` / `我的/服务状态`。
- **用户或管理员动作**：展示服务状态和已知事故。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/service-status`；负责服务为 `Incident Service`。
- **数据实体**：`incidents`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`可观测性/事故管理`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/服务状态”完成“展示服务状态和已知事故”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“可观测性/事故管理”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P09-025-UNIT|P09-025-API|P09-025-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

## 5. 阶段测试与发布

1. 运行 `.\ylven.ps1 py scripts/07_VALIDATE_CONTRACTS.py --phase P09`。
2. 开发期间只运行受影响模块测试；阶段末运行 Go、Web、Android、迁移和契约必要全量测试。
3. 将 `status/P09_FEATURE_STATUS.yaml` 中每个条目更新为最终状态并附证据或原因。
4. 执行 `.\scripts\05_RELEASE_PHASE.ps1 -Phase P09`，再执行 `.\scripts\06_VERIFY_RELEASE.ps1 -Phase P09`。
5. 项目所有者安装 APK、体验当前后台菜单并填写 OWNER_ACCEPTANCE.md；Codex在收到批准前停止，不推进下一阶段。

## 6. 阶段不通过的典型情形

- APK 只是 Shell，Feature Map 中声称的链路没有真实后端或持久化。
- 测试报告没有命令、退出码和日志，只写“已检查”。
- App 直接保存 Sub2API、R2、邮件、Turnstile Secret 或管理 Token。
- 页面、接口、数据库和管理后台使用不同字段含义，或模型能力被写死在 APK。
- 真实外部联调缺失却被标记 IMPLEMENTED，或者非关键外部配置阻塞整个阶段。

<!-- YLVEN_UI_CONTRACT_V1_2 -->

## UI-LOCK：本阶段固定视觉合同（YL-DS-1.2.0）

> 本节是本阶段强制合同，不是参考建议。每次开启新 Codex 会话、上下文压缩后恢复或开始本阶段内任一小版本时，都必须重新读取本节及对应 Work Packet。

### 固定数值

| 固定项 | 数值/规则 |
|---|---|
| 设计系统版本 | `YL-DS-1.2.0` |
| Android 基准画布 | `360×800dp`，效果图 `1080×2400px @3x`，不带手机外框，包含系统栏参考区 |
| 页面左右边距 | 普通页 `16dp`；登录注册页 `24dp` |
| 顶部栏 / 底部导航 | `56dp` / `64dp` 内容高度 |
| 主按钮 | 高 `52dp`，圆角 `14dp`，左右内边距 `20dp` |
| 输入框 | 最小高 `52dp`，圆角 `14dp`，左右内边距 `16dp` |
| 卡片 | 圆角 `16dp`，内边距 `16dp`，边框 `1dp` |
| 间距 | 行内 `8dp`；卡片 `12dp`；区块 `24dp` |
| 字体 | 页标题 `24/32sp 700`；区块 `20/28sp 700`；卡片 `17/24sp 600`；正文 `16/24sp`；次正文 `14/20sp`；说明 `12/16sp` |
| 图标 / 触控 | 常规图标 `24dp`；最小触控区 `48dp` |
| 聊天输入框 | 高 `52–148dp`，圆角 `24dp` |
| 用户消息 | 最大宽度 `84%`，圆角 `18dp`；AI 回答为全宽无大气泡布局 |
| Bottom Sheet / Dialog | 顶部圆角 `28dp` / 圆角 `20dp`、最大宽 `312dp` |
| 浅色主配色 | 背景 `#F6F7FB`；卡片 `#FFFFFF`；主色 `#5B61F6`；主文字 `#101828`；次文字 `#475467`；边框 `#E4E7EC` |
| 管理后台 | 画布 `1440px`；侧栏 `248px`；顶栏 `64px`；内容边距 `24px`；表格行 `48px` |
| 开发者中心 | 画布 `1440px`；侧栏 `240px`；顶栏 `64px`；内容边距 `32px` |

### 本阶段小版本

- [P09-W01 — 工作首页、工具、作品与统一任务中心](../work-packets/P09-W01.md)
- [P09-W02 — 发现页、分类、Prompt、模型实验室和公告](../work-packets/P09-W02.md)
- [P09-W03 — 我的资料、收藏、快捷入口与 AI 偏好](../work-packets/P09-W03.md)
- [P09-W04 — 个人记忆、通知、外观、隐私、更新和反馈](../work-packets/P09-W04.md)
- [P09-W05 — 后台动态配置、内容运营与产品集成验收](../work-packets/P09-W05.md)

### 本阶段页面与状态效果图

- `YL-A-035` — 我的/AI设置（ANDROID；8 状态；P04-W02, P09-W04）
- `YL-A-043` — 我的/服务状态（ANDROID；11 状态；P04-W05, P09-W05）
- `YL-A-059` — 作品菜单（ANDROID；5 状态；P06-W02, P09-W01）
- `YL-A-077` — 工作（ANDROID；7 状态；P09-W01）
- `YL-A-078` — 工作/工具（ANDROID；7 状态；P09-W01）
- `YL-A-079` — 工作/作品（ANDROID；9 状态；P09-W01）
- `YL-A-080` — 工作/任务（ANDROID；9 状态；P09-W01）
- `YL-A-081` — 发现（ANDROID；7 状态；P09-W02）
- `YL-A-082` — 发现/分类（ANDROID；9 状态；P09-W02）
- `YL-A-083` — 发现/Prompt模板（ANDROID；9 状态；P09-W02）
- `YL-A-084` — 发现/模型实验室（ANDROID；9 状态；P09-W02）
- `YL-A-085` — 发现/公告（ANDROID；9 状态；P09-W02）
- `YL-A-086` — 我的（ANDROID；7 状态；P09-W03）
- `YL-A-087` — 我的/个人资料（ANDROID；8 状态；P09-W03）
- `YL-A-088` — 我的/收藏（ANDROID；9 状态；P09-W03）
- `YL-A-089` — 我的/自定义指令（ANDROID；9 状态；P09-W04）
- `YL-A-090` — 我的/个人记忆（ANDROID；8 状态；P09-W04）
- `YL-A-091` — 我的/通知设置（ANDROID；8 状态；P09-W04）
- `YL-A-092` — 我的/外观与语言（ANDROID；8 状态；P09-W04）
- `YL-A-093` — 我的/数据与隐私（ANDROID；8 状态；P09-W05）
- `YL-A-094` — 我的/检查更新（ANDROID；12 状态；P09-W05）
- `YL-A-095` — 我的/帮助与反馈（ANDROID；9 状态；P09-W05）
- `YL-M-007` — 运维/任务队列（ADMIN；8 状态；P00-W02, P09-W01, P12-W04）
- `YL-M-036` — 认证与用户/用户详情（ADMIN；11 状态；P01-W04, P02-W03, P09-W03）
- `YL-M-064` — 模型与供应商/默认配置（ADMIN；11 状态；P04-W02, P09-W04）
- `YL-M-106` — 作品中心/作品详情（ADMIN；11 状态；P07-W03, P09-W01）
- `YL-M-116` — 内容运营/工作页配置（ADMIN；11 状态；P09-W01）
- `YL-M-117` — AI工具/工具目录（ADMIN；11 状态；P09-W01）
- `YL-M-118` — 作品中心/作品列表（ADMIN；8 状态；P09-W01）
- `YL-M-119` — 内容运营/发现页（ADMIN；8 状态；P09-W02, P09-W03）
- `YL-M-120` — 内容运营/Prompt模板（ADMIN；11 状态；P09-W02）
- `YL-M-121` — 模型与供应商/模型展示（ADMIN；8 状态；P09-W02）
- `YL-M-122` — 内容运营/公告（ADMIN；8 状态；P09-W02）
- `YL-M-123` — 内容运营/收藏统计（ADMIN；8 状态；P09-W03）
- `YL-M-124` — 对话与内容/指令策略（ADMIN；11 状态；P09-W04）
- `YL-M-125` — 对话与内容/记忆管理（ADMIN；8 状态；P09-W04）
- `YL-M-126` — 通知中心/用户设置（ADMIN；8 状态；P09-W04）
- `YL-M-127` — 系统设置/默认外观（ADMIN；11 状态；P09-W04）
- `YL-M-128` — 安全与审计/数据导出（ADMIN；8 状态；P09-W05）
- `YL-M-129` — 安全与审计/删除请求（ADMIN；8 状态；P09-W05）
- `YL-M-130` — 系统运营/App版本（ADMIN；11 状态；P09-W05, P13-W03）
- `YL-M-131` — 系统运营/反馈工单（ADMIN；8 状态；P09-W05）
- `YL-M-132` — 可观测性/事故管理（ADMIN；7 状态；P09-W05）
- `YL-W-004` — APK 下载与更新页（WEB；7 状态；P09-W05, P13-W03）
- `YL-W-005` — 公开服务状态页（WEB；8 状态；P04-W05, P09-W05, P12-W03）

本阶段共绑定 **45 个页面、394 个状态效果图**。具体 State ID、图片路径、批准状态与 SHA-256 必须读取：

- `contracts/ui-state-catalog.csv`
- `contracts/mockup-manifest.csv`
- `ui/pages/<surface>/<PageID>.yaml`

### 强制防漂移规则

1. 页面禁止自行写入未登记的字号、圆角、颜色、间距、高度和阴影；只能调用设计 Token 和公共组件。
2. 页面和每个适用状态都必须有独立 Page/State ID；不能用一张成功态效果图推测所有异常状态。
3. 效果图未批准前不得完成视觉实现；错误、离线、超时、空态、成功提示和操作失败不得由 Codex自由设计。
4. 效果图只约束视觉；功能只依据 Feature ID。虚拟样例文字和按钮不构成需求。
5. 阶段发布必须包含 `UI_SCREENSHOT_INDEX.csv`、`VISUAL_DIFF_REPORT.md` 与 `UI_CONTRACT_REPORT.json`。
6. 真机截图与批准效果图存在未解释差异时，本阶段不得标记完成。

<!-- YLVEN_V1_6_CONTINUITY_RULES -->
## V1.6 强制版本连续性、关闭记录与执行环境规则

1. **新会话先恢复，不猜版本。** 新 Codex 会话、上下文压缩恢复、切换分支或 `git pull` 后，第一条命令固定为 `.\ylven.ps1 resume`。只能开发控制器输出的 `CURRENT_PHASE.yaml` 与 `CURRENT_WORK_PACKET.yaml`；聊天中的“继续下个版本”没有状态效力。
2. **定义与状态严格分离。** `contracts/work-packet-map.yaml` 只定义范围；`status/WORK_PACKET_STATUS.yaml` 是唯一可变工作包状态表；禁止再创建第二份当前阶段、当前工作包、状态表、历史或版本总账。
3. **代码写完不等于工作包关闭。** 当前包绑定的 Feature 必须具有最终状态、原因/证据和真实测试记录，代码与测试必须形成干净的实现 Commit，然后执行 `.\ylven.ps1 close-packet -Packet <当前WorkPacketID>`。控制器追加哈希链 `WORK_PACKET_HISTORY.jsonl`、更新运行状态、创建状态 Commit、尝试推送并机械选择下一包。
4. **外部原因延期必须留痕。** 只有真实外部依赖才可执行 `.\ylven.ps1 defer-packet -Packet <当前WorkPacketID> -Reason <具体原因>`；不得用延期掩盖未实现代码、未运行测试或缺失证据。
5. **阶段完成不等于正式版本关闭。** 全部工作包最终化后，阶段只能进入 `READY_FOR_RELEASE`。先执行 `.\ylven.ps1 release -Phase <当前阶段>`，让 GitHub Actions 对精确 Commit 构建、模拟器点击测试、升级安装、截图和视觉回归，并交付同一 CI Artifact。
6. **项目所有者批准后才关闭正式版本。** `OWNER_ACCEPTANCE.md` 含 `- Result: APPROVED`，且版本、Commit、CI Run、Artifact、APK SHA-256 一致后，执行 `.\ylven.ps1 close-release -Phase <当前阶段>`。控制器创建/校验不可变 Tag、追加哈希链 `RELEASE_LEDGER.jsonl`、创建状态 Commit并推进到相邻阶段第一个未完成包。
7. **仓库公开是项目事实，不是阻塞。** 当前仓库由项目所有者确认为 Public。每次提交、推送和发布前必须运行公开仓库 Secret 门禁；服务器地址、SSH 信息、密钥、签名材料、管理员一次性密码、Cloudflare/数据库/API 凭据只能放在本地忽略目录、GitHub Environment 或服务器秘密存储中。
8. **首次空仓库是正常初始化场景。** 缺少 `.git`、远端为空、没有默认分支时运行 `.\ylven.ps1 bootstrap`；Bootstrap 自动初始化 `main`、绑定唯一 `origin`、完成安全扫描并尝试首次推送。推送鉴权失败只阻塞远程 CI，不阻塞本地合同和代码工作。
9. **本机是轻量控制端。** 本机只要求 PowerShell、Git、uv、Spec Kit、OpenSSH，发布下载时使用 GitHub CLI；缺少 Java、Go、Gradle、ADB、Android SDK、Docker、Node 或系统 Python不得列为业务阻塞。构建、模拟器和视觉回归在 GitHub Actions；Docker、数据库和 staging 集成在已连接线上服务器执行。
10. **快速执行优先。** 每个工作包只做一次必要预检，随后直接编码并运行受影响测试；输入未变化时禁止重复全仓库审计。产出优先级固定为：可运行代码 → 自动测试 → 必需合同 → 必需说明。
11. **每版交付必须可追溯。** 桌面交付只能来自通过 CI 的精确 Artifact，并包含计划功能、完成功能、自动测试、项目所有者测试清单、截图索引、视觉差异、端点与 DNS 状态、Build Info、CI Provenance、已知问题和 SHA-256。
