# P04 多模型目录、推理强度、路由与对比回答

> 本阶段包含 **30 个 Feature ID**。本文件与 `contracts/feature-map.yaml` 一致；表中任何条目都不能仅以页面占位、静态返回或聊天声明视为完成。

## 1. 阶段目标

动态管理 OpenAI、Claude、Grok 等模型，按真实能力显示选项，支持会话级/单条切换、分支、对比和透明回退。

## 2. 阶段边界

- 只实现本文件列出的 Feature ID；下一阶段内容不得为了“顺手”提前改变数据契约。
- 每个可见功能必须同时完成必要的 Android/网页状态、后端接口或内部事件、持久化、管理后台控制、权限、日志指标和测试。
- 外部凭据缺失时完成 Adapter、契约 mock、feature flag 和 Owner Actions；仅真实联调条目标记 `BLOCKED_EXTERNAL`。
- 不使用永久假数据、固定余额、固定模型列表、静态成功响应或不可追踪占位作品。
- 阶段发布前，所有本阶段 Feature 状态必须是 `IMPLEMENTED`、`DEFERRED_WITH_REASON` 或 `BLOCKED_EXTERNAL`。

## 3. 建议的纵向实施顺序

1. **Admin 纵向切片**：`P04-020`, `P04-021`, `P04-022`, `P04-023`, `P04-024`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
2. **Android 纵向切片**：`P04-001`, `P04-002`, `P04-003`, `P04-005`, `P04-007`, `P04-008`, `P04-009`, `P04-010`, `P04-011`, `P04-013`, `P04-014`, `P04-015`, `P04-016`, `P04-017`, `P04-018`, `P04-030`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
3. **Backend 纵向切片**：`P04-004`, `P04-006`, `P04-012`, `P04-019`, `P04-027`, `P04-028`, `P04-029`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
4. **Operations 纵向切片**：`P04-025`, `P04-026`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。

## 4. Feature 逐项实施合同

### P04-001 — 从后台动态加载可用模型目录

- **产品表面/页面或菜单**：`Android` / `模型选择器`。
- **用户或管理员动作**：从后台动态加载可用模型目录。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/models`；负责服务为 `Model Catalog Service`。
- **数据实体**：`models|model_capabilities`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/模型目录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型选择器”完成“从后台动态加载可用模型目录”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/模型目录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-001-UNIT|P04-001-API|P04-001-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-002 — 按供应商分组展示模型

- **产品表面/页面或菜单**：`Android` / `模型选择器`。
- **用户或管理员动作**：按供应商分组展示模型。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/models?group=provider`；负责服务为 `Model Catalog Service`。
- **数据实体**：`providers|models`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/供应商`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型选择器”完成“按供应商分组展示模型”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/供应商”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-002-UNIT|P04-002-API|P04-002-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-003 — 展示模型用途、速度和能力标签

- **产品表面/页面或菜单**：`Android` / `模型选择器`。
- **用户或管理员动作**：展示模型用途、速度和能力标签。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/models/{modelId}`；负责服务为 `Model Catalog Service`。
- **数据实体**：`models|model_capabilities`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/模型目录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型选择器”完成“展示模型用途、速度和能力标签”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/模型目录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-003-UNIT|P04-003-API|P04-003-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-004 — 只展示实际探测通过的能力

- **产品表面/页面或菜单**：`Backend` / `模型目录`。
- **用户或管理员动作**：只展示实际探测通过的能力。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://verified-capabilities`；负责服务为 `Capability Registry`。
- **数据实体**：`model_capabilities|capability_probe_results`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力探测`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型目录”完成“只展示实际探测通过的能力”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力探测”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-004-UNIT|P04-004-API|P04-004-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-005 — 加载模型支持的推理档位

- **产品表面/页面或菜单**：`Android` / `推理选择器`。
- **用户或管理员动作**：加载模型支持的推理档位。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/models/{modelId}/reasoning-profiles`；负责服务为 `Model Catalog Service`。
- **数据实体**：`reasoning_profiles`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/推理映射`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“推理选择器”完成“加载模型支持的推理档位”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/推理映射”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-005-UNIT|P04-005-API|P04-005-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-006 — 将用户推理档位映射为上游参数

- **产品表面/页面或菜单**：`Backend` / `推理适配`。
- **用户或管理员动作**：将用户推理档位映射为上游参数。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `adapter://reasoning-profile`；负责服务为 `AI Runtime`。
- **数据实体**：`reasoning_profiles|message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/推理映射`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“推理适配”完成“将用户推理档位映射为上游参数”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/推理映射”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-006-UNIT|P04-006-API|P04-006-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-007 — 设置全局默认模型和推理档位

- **产品表面/页面或菜单**：`Android` / `我的/AI设置`。
- **用户或管理员动作**：设置全局默认模型和推理档位。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/mobile/v1/preferences/ai`；负责服务为 `Preference Service`。
- **数据实体**：`user_preferences`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/默认配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/AI设置”完成“设置全局默认模型和推理档位”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/默认配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-007-UNIT|P04-007-API|P04-007-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-008 — 设置当前会话默认模型

- **产品表面/页面或菜单**：`Android` / `会话设置`。
- **用户或管理员动作**：设置当前会话默认模型。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PATCH` `/api/mobile/v1/conversations/{conversationId}/settings`；负责服务为 `Conversation Service`。
- **数据实体**：`conversations`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/会话详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“会话设置”完成“设置当前会话默认模型”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/会话详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-008-UNIT|P04-008-API|P04-008-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-009 — 仅为本次消息临时切换模型

- **产品表面/页面或菜单**：`Android` / `输入区模型条`。
- **用户或管理员动作**：仅为本次消息临时切换模型。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/conversations/{conversationId}/runs`；负责服务为 `AI Runtime`。
- **数据实体**：`message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/模型目录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“输入区模型条”完成“仅为本次消息临时切换模型”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/模型目录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-009-UNIT|P04-009-API|P04-009-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-010 — 每条回答显示真实模型和推理档位

- **产品表面/页面或菜单**：`Android` / `AI 回复标签`。
- **用户或管理员动作**：每条回答显示真实模型和推理档位。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/messages/{messageId}/run-metadata`；负责服务为 `Conversation Service`。
- **数据实体**：`message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/运行记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“AI 回复标签”完成“每条回答显示真实模型和推理档位”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/运行记录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-010-UNIT|P04-010-API|P04-010-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-011 — 使用其他模型重新回答且保留旧答案

- **产品表面/页面或菜单**：`Android` / `消息操作栏`。
- **用户或管理员动作**：使用其他模型重新回答且保留旧答案。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/messages/{messageId}/rerun`；负责服务为 `AI Runtime`。
- **数据实体**：`conversation_branches|message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/分支`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“消息操作栏”完成“使用其他模型重新回答且保留旧答案”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/分支”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-011-UNIT|P04-011-API|P04-011-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-012 — 创建会话回答分支

- **产品表面/页面或菜单**：`Backend` / `会话分支`。
- **用户或管理员动作**：创建会话回答分支。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/conversations/{conversationId}/branches`；负责服务为 `Conversation Service`。
- **数据实体**：`conversation_branches|messages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/分支`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“会话分支”完成“创建会话回答分支”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/分支”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-012-UNIT|P04-012-API|P04-012-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-013 — 选择并切换当前会话分支

- **产品表面/页面或菜单**：`Android` / `会话分支切换`。
- **用户或管理员动作**：选择并切换当前会话分支。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/conversations/{conversationId}/branches`；负责服务为 `Conversation Service`。
- **数据实体**：`conversation_branches`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/分支`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“会话分支切换”完成“选择并切换当前会话分支”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/分支”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-013-UNIT|P04-013-API|P04-013-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-014 — 同时请求多个模型进行回答对比

- **产品表面/页面或菜单**：`Android` / `多模型对比`。
- **用户或管理员动作**：同时请求多个模型进行回答对比。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/comparisons`；负责服务为 `Comparison Orchestrator`。
- **数据实体**：`comparison_groups|message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`AI工具/多模型对比`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`comparison_run`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“多模型对比”完成“同时请求多个模型进行回答对比”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“AI工具/多模型对比”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账；异步任务 comparison_run 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P04-014-UNIT|P04-014-API|P04-014-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-015 — 标签页切换不同模型对比结果

- **产品表面/页面或菜单**：`Android` / `对比结果`。
- **用户或管理员动作**：标签页切换不同模型对比结果。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/comparisons/{comparisonId}`；负责服务为 `Comparison Orchestrator`。
- **数据实体**：`comparison_groups|messages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`AI工具/多模型对比`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“对比结果”完成“标签页切换不同模型对比结果”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“AI工具/多模型对比”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-015-UNIT|P04-015-API|P04-015-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-016 — 采用一个候选回答继续会话

- **产品表面/页面或菜单**：`Android` / `对比结果`。
- **用户或管理员动作**：采用一个候选回答继续会话。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/comparisons/{comparisonId}/adopt`；负责服务为 `Conversation Service`。
- **数据实体**：`conversation_branches|messages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`AI工具/多模型对比`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“对比结果”完成“采用一个候选回答继续会话”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“AI工具/多模型对比”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-016-UNIT|P04-016-API|P04-016-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-017 — 综合多个模型回答

- **产品表面/页面或菜单**：`Android` / `对比结果`。
- **用户或管理员动作**：综合多个模型回答。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/comparisons/{comparisonId}/synthesize`；负责服务为 `Workflow Orchestrator`。
- **数据实体**：`comparison_groups|message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`AI工具/多模型对比`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`synthesis_run`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“对比结果”完成“综合多个模型回答”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“AI工具/多模型对比”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账；异步任务 synthesis_run 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P04-017-UNIT|P04-017-API|P04-017-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-018 — 透明提示模型不可用并请求用户选择回退

- **产品表面/页面或菜单**：`Android` / `模型错误弹层`。
- **用户或管理员动作**：透明提示模型不可用并请求用户选择回退。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/models/{modelId}/availability`；负责服务为 `Model Routing Service`。
- **数据实体**：`model_health_status`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/服务状态`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型错误弹层”完成“透明提示模型不可用并请求用户选择回退”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/服务状态”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-018-UNIT|P04-018-API|P04-018-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-019 — 按会话授权自动回退模型

- **产品表面/页面或菜单**：`Backend` / `模型路由`。
- **用户或管理员动作**：按会话授权自动回退模型。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://model-fallback`；负责服务为 `Model Routing Service`。
- **数据实体**：`routing_policies|route_targets`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/路由策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型路由”完成“按会话授权自动回退模型”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/路由策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-019-UNIT|P04-019-API|P04-019-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-020 — 配置供应商通道和凭据引用

- **产品表面/页面或菜单**：`Admin` / `模型与供应商/通道`。
- **用户或管理员动作**：配置供应商通道和凭据引用。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET|POST` `/admin/v1/provider-channels`；负责服务为 `Provider Service`。
- **数据实体**：`provider_channels|secret_references`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/通道`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与供应商/通道”完成“配置供应商通道和凭据引用”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/通道”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-020-UNIT|P04-020-API|P04-020-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-021 — 配置模型别名、排序和上下架

- **产品表面/页面或菜单**：`Admin` / `模型与供应商/模型目录`。
- **用户或管理员动作**：配置模型别名、排序和上下架。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET|PUT` `/admin/v1/models/{modelId}`；负责服务为 `Model Catalog Service`。
- **数据实体**：`models|model_aliases`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/模型目录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与供应商/模型目录”完成“配置模型别名、排序和上下架”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/模型目录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-021-UNIT|P04-021-API|P04-021-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-022 — 配置模型能力与上下文限制

- **产品表面/页面或菜单**：`Admin` / `模型与供应商/能力`。
- **用户或管理员动作**：配置模型能力与上下文限制。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/admin/v1/models/{modelId}/capabilities`；负责服务为 `Capability Registry`。
- **数据实体**：`model_capabilities`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与供应商/能力”完成“配置模型能力与上下文限制”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-022-UNIT|P04-022-API|P04-022-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-023 — 配置推理档位映射

- **产品表面/页面或菜单**：`Admin` / `模型与供应商/推理映射`。
- **用户或管理员动作**：配置推理档位映射。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/admin/v1/models/{modelId}/reasoning-profiles`；负责服务为 `Model Catalog Service`。
- **数据实体**：`reasoning_profiles`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/推理映射`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与供应商/推理映射”完成“配置推理档位映射”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/推理映射”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-023-UNIT|P04-023-API|P04-023-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-024 — 配置模型路由优先级和主备线路

- **产品表面/页面或菜单**：`Admin` / `模型与供应商/路由策略`。
- **用户或管理员动作**：配置模型路由优先级和主备线路。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/admin/v1/routing-policies/{policyId}`；负责服务为 `Model Routing Service`。
- **数据实体**：`routing_policies|route_targets`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/路由策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与供应商/路由策略”完成“配置模型路由优先级和主备线路”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/路由策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-024-UNIT|P04-024-API|P04-024-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-025 — 执行模型健康探测

- **产品表面/页面或菜单**：`Operations` / `模型健康`。
- **用户或管理员动作**：执行模型健康探测。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/models/{modelId}/health-probe`；负责服务为 `Capability Probe`。
- **数据实体**：`model_health_probes`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/健康探测`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型健康”完成“执行模型健康探测”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/健康探测”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-025-UNIT|P04-025-API|P04-025-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-026 — 执行文本、流式、视觉和工具能力探测

- **产品表面/页面或菜单**：`Operations` / `能力探测`。
- **用户或管理员动作**：执行文本、流式、视觉和工具能力探测。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/models/{modelId}/capability-probe`；负责服务为 `Capability Probe`。
- **数据实体**：`capability_probe_runs|capability_probe_results`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力探测`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“能力探测”完成“执行文本、流式、视觉和工具能力探测”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力探测”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-026-UNIT|P04-026-API|P04-026-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-027 — 按供应商设置独立并发、超时和熔断

- **产品表面/页面或菜单**：`Backend` / `路由运行时`。
- **用户或管理员动作**：按供应商设置独立并发、超时和熔断。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://provider-isolation`；负责服务为 `AI Runtime`。
- **数据实体**：`provider_runtime_policies`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/运行策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“路由运行时”完成“按供应商设置独立并发、超时和熔断”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/运行策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-027-UNIT|P04-027-API|P04-027-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-028 — 记录输入、输出和推理 Token

- **产品表面/页面或菜单**：`Backend` / `用量计量`。
- **用户或管理员动作**：记录输入、输出和推理 Token。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `event://usage.ai-run`；负责服务为 `Usage Service`。
- **数据实体**：`usage_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`商业化/用量记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“用量计量”完成“记录输入、输出和推理 Token”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“商业化/用量记录”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P04-028-UNIT|P04-028-API|P04-028-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-029 — 记录模型价格版本快照

- **产品表面/页面或菜单**：`Backend` / `用量计量`。
- **用户或管理员动作**：记录模型价格版本快照。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `domain://pricing/snapshot`；负责服务为 `Pricing Service`。
- **数据实体**：`usage_price_snapshots|model_price_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`商业化/模型价格`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“用量计量”完成“记录模型价格版本快照”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“商业化/模型价格”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P04-029-UNIT|P04-029-API|P04-029-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P04-030 — 显示模型服务简化状态

- **产品表面/页面或菜单**：`Android` / `我的/服务状态`。
- **用户或管理员动作**：显示模型服务简化状态。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/service-status/models`；负责服务为 `Incident Service`。
- **数据实体**：`model_health_status|incidents`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/事故管理`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“我的/服务状态”完成“显示模型服务简化状态”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/事故管理”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P04-030-UNIT|P04-030-API|P04-030-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

## 5. 阶段测试与发布

1. 运行 `.\ylven.ps1 py scripts/07_VALIDATE_CONTRACTS.py --phase P04`。
2. 开发期间只运行受影响模块测试；阶段末运行 Go、Web、Android、迁移和契约必要全量测试。
3. 将 `status/P04_FEATURE_STATUS.yaml` 中每个条目更新为最终状态并附证据或原因。
4. 执行 `.\scripts\05_RELEASE_PHASE.ps1 -Phase P04`，再执行 `.\scripts\06_VERIFY_RELEASE.ps1 -Phase P04`。
5. 项目所有者安装 APK、体验当前后台菜单并填写 OWNER_ACCEPTANCE.md；Codex在收到批准前停止，不推进下一阶段。

## 6. 阶段不通过的典型情形

- APK 只是 Shell，Feature Map 中声称的链路没有真实后端或持久化。
- 测试报告没有命令、退出码和日志，只写“已检查”。
- App 直接保存 Sub2API、R2、邮件、Turnstile Secret 或管理 Token。
- 页面、接口、数据库和管理后台使用不同字段含义，或模型能力被写死在 APK。
- 真实外部联调缺失却被标记 IMPLEMENTED，或者非关键外部配置阻塞整个阶段。

<!-- YLVEN_UI_CONTRACT_V1_2 -->

## UI-LOCK：本阶段固定视觉合同（YL-DS-1.2.0）

> 本节是本阶段强制合同，不是参考建议。每次开启新 Codex 会话、上下文压缩后恢复或开始本阶段内任一小版本时，都必须重新读取本节及对应 Work Packet。

### 固定数值

| 固定项 | 数值/规则 |
|---|---|
| 设计系统版本 | `YL-DS-1.2.0` |
| Android 基准画布 | `360×800dp`，效果图 `1080×2400px @3x`，不带手机外框，包含系统栏参考区 |
| 页面左右边距 | 普通页 `16dp`；登录注册页 `24dp` |
| 顶部栏 / 底部导航 | `56dp` / `64dp` 内容高度 |
| 主按钮 | 高 `52dp`，圆角 `14dp`，左右内边距 `20dp` |
| 输入框 | 最小高 `52dp`，圆角 `14dp`，左右内边距 `16dp` |
| 卡片 | 圆角 `16dp`，内边距 `16dp`，边框 `1dp` |
| 间距 | 行内 `8dp`；卡片 `12dp`；区块 `24dp` |
| 字体 | 页标题 `24/32sp 700`；区块 `20/28sp 700`；卡片 `17/24sp 600`；正文 `16/24sp`；次正文 `14/20sp`；说明 `12/16sp` |
| 图标 / 触控 | 常规图标 `24dp`；最小触控区 `48dp` |
| 聊天输入框 | 高 `52–148dp`，圆角 `24dp` |
| 用户消息 | 最大宽度 `84%`，圆角 `18dp`；AI 回答为全宽无大气泡布局 |
| Bottom Sheet / Dialog | 顶部圆角 `28dp` / 圆角 `20dp`、最大宽 `312dp` |
| 浅色主配色 | 背景 `#F6F7FB`；卡片 `#FFFFFF`；主色 `#5B61F6`；主文字 `#101828`；次文字 `#475467`；边框 `#E4E7EC` |
| 管理后台 | 画布 `1440px`；侧栏 `248px`；顶栏 `64px`；内容边距 `24px`；表格行 `48px` |
| 开发者中心 | 画布 `1440px`；侧栏 `240px`；顶栏 `64px`；内容边距 `32px` |

### 本阶段小版本

- [P04-W01 — 供应商、模型目录与模型选择器](../work-packets/P04-W01.md)
- [P04-W02 — 推理档位、默认偏好与本条消息覆盖](../work-packets/P04-W02.md)
- [P04-W03 — 回答分支、多模型对比与综合回答](../work-packets/P04-W03.md)
- [P04-W04 — 模型路由、适配器、透明回退和错误处理](../work-packets/P04-W04.md)
- [P04-W05 — 模型健康、能力探测、后台配置与服务状态](../work-packets/P04-W05.md)

### 本阶段页面与状态效果图

- `YL-A-030` — 消息操作栏（ANDROID；13 状态；P03-W04, P03-W05, P04-W02）
- `YL-A-033` — 模型选择器（ANDROID；7 状态；P04-W01）
- `YL-A-034` — 推理选择器（ANDROID；7 状态；P04-W01）
- `YL-A-035` — 我的/AI设置（ANDROID；8 状态；P04-W02, P09-W04）
- `YL-A-036` — 会话设置（ANDROID；8 状态；P04-W02）
- `YL-A-037` — 输入区模型条（ANDROID；13 状态；P04-W02）
- `YL-A-038` — AI 回复标签（ANDROID；13 状态；P04-W02）
- `YL-A-039` — 会话分支切换（ANDROID；9 状态；P04-W03）
- `YL-A-040` — 多模型对比（ANDROID；9 状态；P04-W03）
- `YL-A-041` — 对比结果（ANDROID；9 状态；P04-W03）
- `YL-A-042` — 模型错误弹层（ANDROID；5 状态；P04-W03）
- `YL-A-043` — 我的/服务状态（ANDROID；11 状态；P04-W05, P09-W05）
- `YL-M-018` — 模型与供应商/能力探测（ADMIN；11 状态；P00-W04, P00-W05, P04-W01, P04-W05）
- `YL-M-048` — 对话与内容/会话详情（ADMIN；11 状态；P03-W01, P04-W02）
- `YL-M-051` — 模型与供应商/模型目录（ADMIN；11 状态；P03-W02, P04-W01, P04-W02, P04-W04）
- `YL-M-053` — 对话与内容/运行记录（ADMIN；8 状态；P03-W02, P03-W03, P03-W04, P04-W02）
- `YL-M-062` — 模型与供应商/供应商（ADMIN；8 状态；P04-W01）
- `YL-M-063` — 模型与供应商/推理映射（ADMIN；11 状态；P04-W01, P04-W04）
- `YL-M-064` — 模型与供应商/默认配置（ADMIN；11 状态；P04-W02, P09-W04）
- `YL-M-065` — 对话与内容/分支（ADMIN；8 状态；P04-W02, P04-W03）
- `YL-M-066` — AI工具/多模型对比（ADMIN；8 状态；P04-W03）
- `YL-M-067` — 模型与供应商/服务状态（ADMIN；9 状态；P04-W03）
- `YL-M-068` — 模型与供应商/路由策略（ADMIN；11 状态；P04-W04, P13-W02）
- `YL-M-069` — 模型与供应商/通道（ADMIN；11 状态；P04-W04, P13-W02）
- `YL-M-070` — 模型与供应商/能力（ADMIN；11 状态；P04-W04, P05-W01）
- `YL-M-071` — 模型与供应商/健康探测（ADMIN；7 状态；P04-W05）
- `YL-M-072` — 模型与供应商/运行策略（ADMIN；11 状态；P04-W05）
- `YL-M-073` — 商业化/用量记录（ADMIN；8 状态；P04-W05）
- `YL-M-074` — 商业化/模型价格（ADMIN；8 状态；P04-W05, P10-W03, P10-W05, P11-W04）
- `YL-M-075` — 运维/事故管理（ADMIN；7 状态；P04-W05）
- `YL-W-005` — 公开服务状态页（WEB；8 状态；P04-W05, P09-W05, P12-W03）

本阶段共绑定 **31 个页面、290 个状态效果图**。具体 State ID、图片路径、批准状态与 SHA-256 必须读取：

- `contracts/ui-state-catalog.csv`
- `contracts/mockup-manifest.csv`
- `ui/pages/<surface>/<PageID>.yaml`

### 强制防漂移规则

1. 页面禁止自行写入未登记的字号、圆角、颜色、间距、高度和阴影；只能调用设计 Token 和公共组件。
2. 页面和每个适用状态都必须有独立 Page/State ID；不能用一张成功态效果图推测所有异常状态。
3. 效果图未批准前不得完成视觉实现；错误、离线、超时、空态、成功提示和操作失败不得由 Codex自由设计。
4. 效果图只约束视觉；功能只依据 Feature ID。虚拟样例文字和按钮不构成需求。
5. 阶段发布必须包含 `UI_SCREENSHOT_INDEX.csv`、`VISUAL_DIFF_REPORT.md` 与 `UI_CONTRACT_REPORT.json`。
6. 真机截图与批准效果图存在未解释差异时，本阶段不得标记完成。

<!-- YLVEN_V1_6_CONTINUITY_RULES -->
## V1.6 强制版本连续性、关闭记录与执行环境规则

1. **新会话先恢复，不猜版本。** 新 Codex 会话、上下文压缩恢复、切换分支或 `git pull` 后，第一条命令固定为 `.\ylven.ps1 resume`。只能开发控制器输出的 `CURRENT_PHASE.yaml` 与 `CURRENT_WORK_PACKET.yaml`；聊天中的“继续下个版本”没有状态效力。
2. **定义与状态严格分离。** `contracts/work-packet-map.yaml` 只定义范围；`status/WORK_PACKET_STATUS.yaml` 是唯一可变工作包状态表；禁止再创建第二份当前阶段、当前工作包、状态表、历史或版本总账。
3. **代码写完不等于工作包关闭。** 当前包绑定的 Feature 必须具有最终状态、原因/证据和真实测试记录，代码与测试必须形成干净的实现 Commit，然后执行 `.\ylven.ps1 close-packet -Packet <当前WorkPacketID>`。控制器追加哈希链 `WORK_PACKET_HISTORY.jsonl`、更新运行状态、创建状态 Commit、尝试推送并机械选择下一包。
4. **外部原因延期必须留痕。** 只有真实外部依赖才可执行 `.\ylven.ps1 defer-packet -Packet <当前WorkPacketID> -Reason <具体原因>`；不得用延期掩盖未实现代码、未运行测试或缺失证据。
5. **阶段完成不等于正式版本关闭。** 全部工作包最终化后，阶段只能进入 `READY_FOR_RELEASE`。先执行 `.\ylven.ps1 release -Phase <当前阶段>`，让 GitHub Actions 对精确 Commit 构建、模拟器点击测试、升级安装、截图和视觉回归，并交付同一 CI Artifact。
6. **项目所有者批准后才关闭正式版本。** `OWNER_ACCEPTANCE.md` 含 `- Result: APPROVED`，且版本、Commit、CI Run、Artifact、APK SHA-256 一致后，执行 `.\ylven.ps1 close-release -Phase <当前阶段>`。控制器创建/校验不可变 Tag、追加哈希链 `RELEASE_LEDGER.jsonl`、创建状态 Commit并推进到相邻阶段第一个未完成包。
7. **仓库公开是项目事实，不是阻塞。** 当前仓库由项目所有者确认为 Public。每次提交、推送和发布前必须运行公开仓库 Secret 门禁；服务器地址、SSH 信息、密钥、签名材料、管理员一次性密码、Cloudflare/数据库/API 凭据只能放在本地忽略目录、GitHub Environment 或服务器秘密存储中。
8. **首次空仓库是正常初始化场景。** 缺少 `.git`、远端为空、没有默认分支时运行 `.\ylven.ps1 bootstrap`；Bootstrap 自动初始化 `main`、绑定唯一 `origin`、完成安全扫描并尝试首次推送。推送鉴权失败只阻塞远程 CI，不阻塞本地合同和代码工作。
9. **本机是轻量控制端。** 本机只要求 PowerShell、Git、uv、Spec Kit、OpenSSH，发布下载时使用 GitHub CLI；缺少 Java、Go、Gradle、ADB、Android SDK、Docker、Node 或系统 Python不得列为业务阻塞。构建、模拟器和视觉回归在 GitHub Actions；Docker、数据库和 staging 集成在已连接线上服务器执行。
10. **本机 Git 推送失败时的固定备用路径。** 保持同一 Commit SHA、阶段分支和 `origin` 不变，通过已验证的 SSH/SFTP 将完整 Git Bundle 传到服务器，校验 SHA-256 与 `git bundle verify` 后由服务器推送 GitHub；不得把 Token、密钥或 Secret 放入仓库、Bundle、命令行或日志，推送后必须回读 GitHub 分支 SHA。
10. **快速执行优先。** 每个工作包只做一次必要预检，随后直接编码并运行受影响测试；输入未变化时禁止重复全仓库审计。产出优先级固定为：可运行代码 → 自动测试 → 必需合同 → 必需说明。
11. **每版交付必须可追溯。** 桌面交付只能来自通过 CI 的精确 Artifact，并包含计划功能、完成功能、自动测试、项目所有者测试清单、截图索引、视觉差异、端点与 DNS 状态、Build Info、CI Provenance、已知问题和 SHA-256。

<!-- CO_P03_001_GLOBAL_PRODUCTION_RULES -->
## 正式运营、无 Demo、用户文案与分布式就绪规则

1. 前后端必须按真实运营实现；禁止 Demo、Stub、Mock、静态假数据、伪成功接口和仅内存业务状态。
2. 普通用户界面不得出现 streaming、connecting、provider error、request id、SSE、Sub2API、debug、demo、mock 等技术或测试词；必须映射为普通用户可理解且可恢复的文案。
3. 页面必须使用已批准效果图和固定 UI Token；样例文字不创造功能，功能不得因效果图未展示而删除。
4. 当前可单服务器运行，但 API、AI Runtime、SSE、任务和文件必须满足未来多实例接管；正式状态不得只保存在单节点。
5. 发布前必须通过真实数据库/接口、权限、幂等、迁移、普通用户文案、模拟器交互、截图视觉和 CI Artifact 来源验收。
6. 多写实际代码与测试，少做重复审计；输入未变化时不得循环执行全仓库检查代替实现。

