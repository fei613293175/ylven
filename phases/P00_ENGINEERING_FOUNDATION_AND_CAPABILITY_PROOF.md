# P00 工程基座、部署骨架与上游能力证明

> 本阶段包含 **24 个 Feature ID**。本文件与 `contracts/feature-map.yaml` 一致；表中任何条目都不能仅以页面占位、静态返回或聊天声明视为完成。

## 1. 阶段目标

建立可持续开发的仓库、Android/后端/后台工程骨架、基础设施契约、Sub2API 能力探测与真实构建发布链路。

## 2. 阶段边界

- 只实现本文件列出的 Feature ID；下一阶段内容不得为了“顺手”提前改变数据契约。
- 每个可见功能必须同时完成必要的 Android/网页状态、后端接口或内部事件、持久化、管理后台控制、权限、日志指标和测试。
- 外部凭据缺失时完成 Adapter、契约 mock、feature flag 和 Owner Actions；仅真实联调条目标记 `BLOCKED_EXTERNAL`。
- 不使用永久假数据、固定余额、固定模型列表、静态成功响应或不可追踪占位作品。
- 阶段发布前，所有本阶段 Feature 状态必须是 `IMPLEMENTED`、`DEFERRED_WITH_REASON` 或 `BLOCKED_EXTERNAL`。

## 3. 建议的纵向实施顺序

1. **Admin 纵向切片**：`P00-023`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
2. **Android 纵向切片**：`P00-003`, `P00-004`, `P00-005`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
3. **Backend 纵向切片**：`P00-006`, `P00-007`, `P00-008`, `P00-009`, `P00-010`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
4. **Data 纵向切片**：`P00-011`, `P00-012`, `P00-013`, `P00-014`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
5. **Developer Web 纵向切片**：`P00-024`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
6. **Engineering 纵向切片**：`P00-001`, `P00-002`, `P00-015`, `P00-016`, `P00-018`, `P00-019`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
7. **Operations 纵向切片**：`P00-017`, `P00-020`, `P00-021`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
8. **Security 纵向切片**：`P00-022`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。

## 4. Feature 逐项实施合同

### P00-001 — 建立仓库目录与模块边界

- **产品表面/页面或菜单**：`Engineering` / `Repository`。
- **用户或管理员动作**：建立仓库目录与模块边界。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `repo://layout`；负责服务为 `Build System`。
- **数据实体**：`repository_modules`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统设置/工程信息`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Repository”完成“建立仓库目录与模块边界”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统设置/工程信息”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-001-UNIT|P00-001-API|P00-001-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-002 — 建立环境配置分层

- **产品表面/页面或菜单**：`Engineering` / `Configuration`。
- **用户或管理员动作**：建立环境配置分层。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `config://environments`；负责服务为 `Configuration Service`。
- **数据实体**：`system_configs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统设置/环境配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Configuration”完成“建立环境配置分层”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统设置/环境配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-002-UNIT|P00-002-API|P00-002-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-003 — 建立 Android Compose 应用壳

- **产品表面/页面或菜单**：`Android` / `App Shell`。
- **用户或管理员动作**：建立 Android Compose 应用壳。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `android://app-shell`；负责服务为 `Android App`。
- **数据实体**：`app_builds`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`App发布/构建记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“App Shell”完成“建立 Android Compose 应用壳”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“App发布/构建记录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-003-UNIT|P00-003-API|P00-003-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-004 — 建立 YLVEN 浅色与深色主题 Token

- **产品表面/页面或菜单**：`Android` / `Design System`。
- **用户或管理员动作**：建立 YLVEN 浅色与深色主题 Token。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `android://design-tokens`；负责服务为 `Android Design System`。
- **数据实体**：`design_tokens`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`内容运营/设计配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Design System”完成“建立 YLVEN 浅色与深色主题 Token”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“内容运营/设计配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-004-UNIT|P00-004-API|P00-004-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-005 — 建立四栏导航与占位态

- **产品表面/页面或菜单**：`Android` / `首页/工作/发现/我的`。
- **用户或管理员动作**：建立四栏导航与占位态。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `android://navigation`；负责服务为 `Android App`。
- **数据实体**：`feature_flags`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`发现与首页/导航配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“首页/工作/发现/我的”完成“建立四栏导航与占位态”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“发现与首页/导航配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-005-UNIT|P00-005-API|P00-005-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-006 — 建立 Core API 服务壳

- **产品表面/页面或菜单**：`Backend` / `Core API`。
- **用户或管理员动作**：建立 Core API 服务壳。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/health`；负责服务为 `Core API`。
- **数据实体**：`service_instances`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/服务健康`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Core API”完成“建立 Core API 服务壳”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/服务健康”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-006-UNIT|P00-006-API|P00-006-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-007 — 建立 AI Runtime 服务壳

- **产品表面/页面或菜单**：`Backend` / `AI Runtime`。
- **用户或管理员动作**：建立 AI Runtime 服务壳。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/runtime/health`；负责服务为 `AI Runtime`。
- **数据实体**：`service_instances`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/服务健康`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“AI Runtime”完成“建立 AI Runtime 服务壳”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/服务健康”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-007-UNIT|P00-007-API|P00-007-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-008 — 建立 Developer Gateway 服务壳

- **产品表面/页面或菜单**：`Backend` / `Developer Gateway`。
- **用户或管理员动作**：建立 Developer Gateway 服务壳。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/developer/health`；负责服务为 `Developer Gateway`。
- **数据实体**：`service_instances`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/服务健康`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Developer Gateway”完成“建立 Developer Gateway 服务壳”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/服务健康”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-008-UNIT|P00-008-API|P00-008-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-009 — 建立 Worker 与队列消费框架

- **产品表面/页面或菜单**：`Backend` / `Worker`。
- **用户或管理员动作**：建立 Worker 与队列消费框架。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/worker/health`；负责服务为 `Worker`。
- **数据实体**：`jobs|job_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/任务队列`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Worker”完成“建立 Worker 与队列消费框架”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/任务队列”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-009-UNIT|P00-009-API|P00-009-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-010 — 建立 Scheduler 与分布式锁

- **产品表面/页面或菜单**：`Backend` / `Scheduler`。
- **用户或管理员动作**：建立 Scheduler 与分布式锁。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/scheduler/health`；负责服务为 `Scheduler`。
- **数据实体**：`scheduled_jobs|distributed_locks`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/定时任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Scheduler”完成“建立 Scheduler 与分布式锁”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/定时任务”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-010-UNIT|P00-010-API|P00-010-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-011 — 建立 PostgreSQL 迁移体系

- **产品表面/页面或菜单**：`Data` / `Database`。
- **用户或管理员动作**：建立 PostgreSQL 迁移体系。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `db://migrations`；负责服务为 `Data Platform`。
- **数据实体**：`schema_migrations`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/数据库迁移`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Database”完成“建立 PostgreSQL 迁移体系”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/数据库迁移”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-011-UNIT|P00-011-API|P00-011-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-012 — 建立 Redis 缓存与限流连接

- **产品表面/页面或菜单**：`Data` / `Redis`。
- **用户或管理员动作**：建立 Redis 缓存与限流连接。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `redis://health`；负责服务为 `Data Platform`。
- **数据实体**：`cache_namespaces`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/Redis`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Redis”完成“建立 Redis 缓存与限流连接”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/Redis”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-012-UNIT|P00-012-API|P00-012-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-013 — 建立 NATS JetStream 事件总线

- **产品表面/页面或菜单**：`Data` / `NATS`。
- **用户或管理员动作**：建立 NATS JetStream 事件总线。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `nats://health`；负责服务为 `Event Platform`。
- **数据实体**：`event_streams`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/事件流`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“NATS”完成“建立 NATS JetStream 事件总线”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/事件流”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-013-UNIT|P00-013-API|P00-013-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-014 — 建立 Cloudflare R2 存储适配器

- **产品表面/页面或菜单**：`Data` / `Object Storage`。
- **用户或管理员动作**：建立 Cloudflare R2 存储适配器。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `r2://health`；负责服务为 `File Service`。
- **数据实体**：`storage_buckets`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/存储配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Object Storage”完成“建立 Cloudflare R2 存储适配器”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/存储配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-014-UNIT|P00-014-API|P00-014-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-015 — 建立 OpenAPI 合同与代码生成

- **产品表面/页面或菜单**：`Engineering` / `API Contract`。
- **用户或管理员动作**：建立 OpenAPI 合同与代码生成。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `contract://openapi`；负责服务为 `Contract Tooling`。
- **数据实体**：`api_contracts`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统设置/API版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Contract”完成“建立 OpenAPI 合同与代码生成”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统设置/API版本”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-015-UNIT|P00-015-API|P00-015-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-016 — 建立 Feature Map 机器校验

- **产品表面/页面或菜单**：`Engineering` / `Feature Contract`。
- **用户或管理员动作**：建立 Feature Map 机器校验。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `contract://features`；负责服务为 `Contract Tooling`。
- **数据实体**：`feature_contracts`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`系统设置/功能追踪`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Feature Contract”完成“建立 Feature Map 机器校验”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“系统设置/功能追踪”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-016-UNIT|P00-016-API|P00-016-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-017 — 建立结构化日志、指标与追踪

- **产品表面/页面或菜单**：`Operations` / `Observability`。
- **用户或管理员动作**：建立结构化日志、指标与追踪。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/internal/metrics`；负责服务为 `Observability`。
- **数据实体**：`audit_logs|service_metrics|traces`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`可观测性/总览`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Observability”完成“建立结构化日志、指标与追踪”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“可观测性/总览”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-017-UNIT|P00-017-API|P00-017-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-018 — 建立 GitHub Actions 基础 CI

- **产品表面/页面或菜单**：`Engineering` / `CI`。
- **用户或管理员动作**：建立 GitHub Actions 基础 CI。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `ci://quality`；负责服务为 `CI Pipeline`。
- **数据实体**：`ci_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`运维/构建记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“CI”完成“建立 GitHub Actions 基础 CI”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“运维/构建记录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-018-UNIT|P00-018-API|P00-018-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-019 — 建立阶段发布与桌面交付脚本

- **产品表面/页面或菜单**：`Engineering` / `Release`。
- **用户或管理员动作**：建立阶段发布与桌面交付脚本。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `script://release-phase`；负责服务为 `Release Tooling`。
- **数据实体**：`release_manifests`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`App发布/阶段交付`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Release”完成“建立阶段发布与桌面交付脚本”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“App发布/阶段交付”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-019-UNIT|P00-019-API|P00-019-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-020 — 探测 Sub2API 文本与流式能力

- **产品表面/页面或菜单**：`Operations` / `Capability Probe`。
- **用户或管理员动作**：探测 Sub2API 文本与流式能力。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/probes/sub2api/chat`；负责服务为 `Capability Probe`。
- **数据实体**：`provider_channels|capability_probe_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力探测`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Capability Probe”完成“探测 Sub2API 文本与流式能力”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力探测”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-020-UNIT|P00-020-API|P00-020-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-021 — 探测 Sub2API 视觉、文件与图片能力

- **产品表面/页面或菜单**：`Operations` / `Capability Probe`。
- **用户或管理员动作**：探测 Sub2API 视觉、文件与图片能力。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/probes/sub2api/multimodal`；负责服务为 `Capability Probe`。
- **数据实体**：`model_capabilities|capability_probe_results`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力探测`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Capability Probe”完成“探测 Sub2API 视觉、文件与图片能力”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力探测”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-021-UNIT|P00-021-API|P00-021-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-022 — 建立秘密管理与示例环境文件

- **产品表面/页面或菜单**：`Security` / `Secrets`。
- **用户或管理员动作**：建立秘密管理与示例环境文件。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `secret://inventory`；负责服务为 `Security Service`。
- **数据实体**：`secret_references`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`安全与审计/秘密状态`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Secrets”完成“建立秘密管理与示例环境文件”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“安全与审计/秘密状态”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-022-UNIT|P00-022-API|P00-022-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-023 — 建立后台 Vue 应用壳和 RBAC 路由

- **产品表面/页面或菜单**：`Admin` / `管理后台`。
- **用户或管理员动作**：建立后台 Vue 应用壳和 RBAC 路由。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `admin://shell`；负责服务为 `Admin Web`。
- **数据实体**：`admin_routes`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`管理员与权限/菜单`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“管理后台”完成“建立后台 Vue 应用壳和 RBAC 路由”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“管理员与权限/菜单”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-023-UNIT|P00-023-API|P00-023-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P00-024 — 建立开发者门户应用壳

- **产品表面/页面或菜单**：`Developer Web` / `开发者中心`。
- **用户或管理员动作**：建立开发者门户应用壳。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `developer-web://shell`；负责服务为 `Developer Web`。
- **数据实体**：`developer_routes`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/门户配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“开发者中心”完成“建立开发者门户应用壳”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/门户配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P00-024-UNIT|P00-024-API|P00-024-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

## 5. 阶段测试与发布

1. 运行 `.\ylven.ps1 py scripts/07_VALIDATE_CONTRACTS.py --phase P00`。
2. 开发期间只运行受影响模块测试；阶段末运行 Go、Web、Android、迁移和契约必要全量测试。
3. 将 `status/P00_FEATURE_STATUS.yaml` 中每个条目更新为最终状态并附证据或原因。
4. 执行 `.\scripts\05_RELEASE_PHASE.ps1 -Phase P00`，再执行 `.\scripts\06_VERIFY_RELEASE.ps1 -Phase P00`。
5. 项目所有者安装 APK、体验当前后台菜单并填写 OWNER_ACCEPTANCE.md；Codex在收到批准前停止，不推进下一阶段。

## 6. 阶段不通过的典型情形

- APK 只是 Shell，Feature Map 中声称的链路没有真实后端或持久化。
- 测试报告没有命令、退出码和日志，只写“已检查”。
- App 直接保存 Sub2API、R2、邮件、Turnstile Secret 或管理 Token。
- 页面、接口、数据库和管理后台使用不同字段含义，或模型能力被写死在 APK。
- 真实外部联调缺失却被标记 IMPLEMENTED，或者非关键外部配置阻塞整个阶段。

<!-- YLVEN_UI_CONTRACT_V1_2 -->

## UI-LOCK：本阶段固定视觉合同（YL-DS-1.2.0）

> 本节是本阶段强制合同，不是参考建议。每次开启新 Codex 会话、上下文压缩后恢复或开始本阶段内任一小版本时，都必须重新读取本节及对应 Work Packet。

### 固定数值

| 固定项 | 数值/规则 |
|---|---|
| 设计系统版本 | `YL-DS-1.2.0` |
| Android 基准画布 | `360×800dp`，效果图 `1080×2400px @3x`，不带手机外框，包含系统栏参考区 |
| 页面左右边距 | 普通页 `16dp`；登录注册页 `24dp` |
| 顶部栏 / 底部导航 | `56dp` / `64dp` 内容高度 |
| 主按钮 | 高 `52dp`，圆角 `14dp`，左右内边距 `20dp` |
| 输入框 | 最小高 `52dp`，圆角 `14dp`，左右内边距 `16dp` |
| 卡片 | 圆角 `16dp`，内边距 `16dp`，边框 `1dp` |
| 间距 | 行内 `8dp`；卡片 `12dp`；区块 `24dp` |
| 字体 | 页标题 `24/32sp 700`；区块 `20/28sp 700`；卡片 `17/24sp 600`；正文 `16/24sp`；次正文 `14/20sp`；说明 `12/16sp` |
| 图标 / 触控 | 常规图标 `24dp`；最小触控区 `48dp` |
| 聊天输入框 | 高 `52–148dp`，圆角 `24dp` |
| 用户消息 | 最大宽度 `84%`，圆角 `18dp`；AI 回答为全宽无大气泡布局 |
| Bottom Sheet / Dialog | 顶部圆角 `28dp` / 圆角 `20dp`、最大宽 `312dp` |
| 浅色主配色 | 背景 `#F6F7FB`；卡片 `#FFFFFF`；主色 `#5B61F6`；主文字 `#101828`；次文字 `#475467`；边框 `#E4E7EC` |
| 管理后台 | 画布 `1440px`；侧栏 `248px`；顶栏 `64px`；内容边距 `24px`；表格行 `48px` |
| 开发者中心 | 画布 `1440px`；侧栏 `240px`；顶栏 `64px`；内容边距 `32px` |

### 本阶段小版本

- [P00-W01 — 仓库、配置、合同与工程基线](../work-packets/P00-W01.md)
- [P00-W02 — Android 设计系统与四栏应用壳](../work-packets/P00-W02.md)
- [P00-W03 — 核心服务、AI Runtime 与任务进程壳](../work-packets/P00-W03.md)
- [P00-W04 — 数据基础设施、可观测性、CI 与阶段发布](../work-packets/P00-W04.md)
- [P00-W05 — Sub2API 能力探测、秘密边界、后台与开发者门户壳](../work-packets/P00-W05.md)

### 本阶段页面与状态效果图

- `YL-A-001` — App Shell（ANDROID；7 状态；P00-W01）
- `YL-A-002` — Design System（ANDROID；1 状态；P00-W01）
- `YL-A-003` — 首页/工作/发现/我的（ANDROID；7 状态；P00-W01）
- `YL-M-001` — 系统设置/工程信息（ADMIN；8 状态；P00-W01）
- `YL-M-002` — 系统设置/环境配置（ADMIN；11 状态；P00-W01）
- `YL-M-003` — App发布/构建记录（ADMIN；8 状态；P00-W01）
- `YL-M-004` — 内容运营/设计配置（ADMIN；11 状态；P00-W01）
- `YL-M-005` — 发现与首页/导航配置（ADMIN；11 状态；P00-W01）
- `YL-M-006` — 运维/服务健康（ADMIN；7 状态；P00-W02）
- `YL-M-007` — 运维/任务队列（ADMIN；8 状态；P00-W02, P09-W01, P12-W04）
- `YL-M-008` — 运维/定时任务（ADMIN；8 状态；P00-W02）
- `YL-M-009` — 运维/数据库迁移（ADMIN；8 状态；P00-W03, P13-W01）
- `YL-M-010` — 运维/Redis（ADMIN；8 状态；P00-W03）
- `YL-M-011` — 运维/事件流（ADMIN；8 状态；P00-W03）
- `YL-M-012` — 文件与存储/存储配置（ADMIN；11 状态；P00-W03）
- `YL-M-013` — 系统设置/API版本（ADMIN；11 状态；P00-W03）
- `YL-M-014` — 系统设置/功能追踪（ADMIN；8 状态；P00-W04）
- `YL-M-015` — 可观测性/总览（ADMIN；7 状态；P00-W04）
- `YL-M-016` — 运维/构建记录（ADMIN；8 状态；P00-W04）
- `YL-M-017` — App发布/阶段交付（ADMIN；8 状态；P00-W04）
- `YL-M-018` — 模型与供应商/能力探测（ADMIN；11 状态；P00-W04, P00-W05, P04-W01, P04-W05）
- `YL-M-019` — 安全与审计/秘密状态（ADMIN；8 状态；P00-W05）
- `YL-M-020` — 管理员与权限/菜单（ADMIN；8 状态；P00-W05）
- `YL-M-021` — 开发者平台/门户配置（ADMIN；11 状态；P00-W05）
- `YL-D-001` — 开发者中心（DEVELOPER；7 状态；P00-W05）
- `YL-DS-001` — 颜色、主题与对比度基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-002` — 字体、字号、行高与字重基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-003` — 间距、网格、圆角、边框与阴影基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-004` — 按钮、图标、Chip 与分段控件基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-005` — 输入框、验证码、搜索和表单基准板（DESIGN_SYSTEM；5 状态；P00-W01）
- `YL-DS-006` — 顶部栏、底部导航、侧栏和标签页基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-007` — 卡片、列表、表格、筛选和分页基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-008` — Dialog、Bottom Sheet、Snackbar 与菜单基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-009` — Loading、Skeleton、Empty、Offline 与 Error 基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-010` — 聊天消息、模型标签、代码、表格和引用基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-011` — 文件、附件、上传、解析和资料库基准板（DESIGN_SYSTEM；1 状态；P00-W01）
- `YL-DS-012` — 图片工作台、任务、结果和编辑基准板（DESIGN_SYSTEM；5 状态；P00-W01）
- `YL-DS-013` — PPT 工作台、缩略图、预览和后台/开发者组件基准板（DESIGN_SYSTEM；1 状态；P00-W01）

本阶段共绑定 **38 个页面、230 个状态效果图**。具体 State ID、图片路径、批准状态与 SHA-256 必须读取：

- `contracts/ui-state-catalog.csv`
- `contracts/mockup-manifest.csv`
- `ui/pages/<surface>/<PageID>.yaml`

### 强制防漂移规则

1. 页面禁止自行写入未登记的字号、圆角、颜色、间距、高度和阴影；只能调用设计 Token 和公共组件。
2. 页面和每个适用状态都必须有独立 Page/State ID；不能用一张成功态效果图推测所有异常状态。
3. 效果图未批准前不得完成视觉实现；错误、离线、超时、空态、成功提示和操作失败不得由 Codex自由设计。
4. 效果图只约束视觉；功能只依据 Feature ID。虚拟样例文字和按钮不构成需求。
5. 阶段发布必须包含 `UI_SCREENSHOT_INDEX.csv`、`VISUAL_DIFF_REPORT.md` 与 `UI_CONTRACT_REPORT.json`。
6. 真机截图与批准效果图存在未解释差异时，本阶段不得标记完成。

<!-- YLVEN_V1_6_CONTINUITY_RULES -->
## V1.6 强制版本连续性、关闭记录与执行环境规则

1. **新会话先恢复，不猜版本。** 新 Codex 会话、上下文压缩恢复、切换分支或 `git pull` 后，第一条命令固定为 `.\ylven.ps1 resume`。只能开发控制器输出的 `CURRENT_PHASE.yaml` 与 `CURRENT_WORK_PACKET.yaml`；聊天中的“继续下个版本”没有状态效力。
2. **定义与状态严格分离。** `contracts/work-packet-map.yaml` 只定义范围；`status/WORK_PACKET_STATUS.yaml` 是唯一可变工作包状态表；禁止再创建第二份当前阶段、当前工作包、状态表、历史或版本总账。
3. **代码写完不等于工作包关闭。** 当前包绑定的 Feature 必须具有最终状态、原因/证据和真实测试记录，代码与测试必须形成干净的实现 Commit，然后执行 `.\ylven.ps1 close-packet -Packet <当前WorkPacketID>`。控制器追加哈希链 `WORK_PACKET_HISTORY.jsonl`、更新运行状态、创建状态 Commit、尝试推送并机械选择下一包。
4. **外部原因延期必须留痕。** 只有真实外部依赖才可执行 `.\ylven.ps1 defer-packet -Packet <当前WorkPacketID> -Reason <具体原因>`；不得用延期掩盖未实现代码、未运行测试或缺失证据。
5. **阶段完成不等于正式版本关闭。** 全部工作包最终化后，阶段只能进入 `READY_FOR_RELEASE`。先执行 `.\ylven.ps1 release -Phase <当前阶段>`，让 GitHub Actions 对精确 Commit 构建、模拟器点击测试、升级安装、截图和视觉回归，并交付同一 CI Artifact。
6. **项目所有者批准后才关闭正式版本。** `OWNER_ACCEPTANCE.md` 含 `- Result: APPROVED`，且版本、Commit、CI Run、Artifact、APK SHA-256 一致后，执行 `.\ylven.ps1 close-release -Phase <当前阶段>`。控制器创建/校验不可变 Tag、追加哈希链 `RELEASE_LEDGER.jsonl`、创建状态 Commit并推进到相邻阶段第一个未完成包。
7. **仓库公开是项目事实，不是阻塞。** 当前仓库由项目所有者确认为 Public。每次提交、推送和发布前必须运行公开仓库 Secret 门禁；服务器地址、SSH 信息、密钥、签名材料、管理员一次性密码、Cloudflare/数据库/API 凭据只能放在本地忽略目录、GitHub Environment 或服务器秘密存储中。
8. **首次空仓库是正常初始化场景。** 缺少 `.git`、远端为空、没有默认分支时运行 `.\ylven.ps1 bootstrap`；Bootstrap 自动初始化 `main`、绑定唯一 `origin`、完成安全扫描并尝试首次推送。推送鉴权失败只阻塞远程 CI，不阻塞本地合同和代码工作。
9. **本机是轻量控制端。** 本机只要求 PowerShell、Git、uv、Spec Kit、OpenSSH，发布下载时使用 GitHub CLI；缺少 Java、Go、Gradle、ADB、Android SDK、Docker、Node 或系统 Python不得列为业务阻塞。构建、模拟器和视觉回归在 GitHub Actions；Docker、数据库和 staging 集成在已连接线上服务器执行。
10. **本机 Git 推送失败时的固定备用路径。** 保持同一 Commit SHA、阶段分支和 `origin` 不变，通过已验证的 SSH/SFTP 将完整 Git Bundle 传到服务器，校验 SHA-256 与 `git bundle verify` 后由服务器推送 GitHub；不得把 Token、密钥或 Secret 放入仓库、Bundle、命令行或日志，推送后必须回读 GitHub 分支 SHA。
10. **快速执行优先。** 每个工作包只做一次必要预检，随后直接编码并运行受影响测试；输入未变化时禁止重复全仓库审计。产出优先级固定为：可运行代码 → 自动测试 → 必需合同 → 必需说明。
11. **每版交付必须可追溯。** 桌面交付只能来自通过 CI 的精确 Artifact，并包含计划功能、完成功能、自动测试、项目所有者测试清单、截图索引、视觉差异、端点与 DNS 状态、Build Info、CI Provenance、已知问题和 SHA-256。
