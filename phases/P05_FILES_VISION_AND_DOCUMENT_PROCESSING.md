# P05 文件资料库、视觉输入与 Office/PDF 解析

> 本阶段包含 **25 个 Feature ID**。本文件与 `contracts/feature-map.yaml` 一致；表中任何条目都不能仅以页面占位、静态返回或聊天声明视为完成。

## 1. 阶段目标

通过 R2 直传、文件安全校验和异步解析实现识图、PDF/Word/PPT/Excel 分析及供应商临时引用管理。

## 2. 阶段边界

- 只实现本文件列出的 Feature ID；下一阶段内容不得为了“顺手”提前改变数据契约。
- 每个可见功能必须同时完成必要的 Android/网页状态、后端接口或内部事件、持久化、管理后台控制、权限、日志指标和测试。
- 外部凭据缺失时完成 Adapter、契约 mock、feature flag 和 Owner Actions；仅真实联调条目标记 `BLOCKED_EXTERNAL`。
- 不使用永久假数据、固定余额、固定模型列表、静态成功响应或不可追踪占位作品。
- 阶段发布前，所有本阶段 Feature 状态必须是 `IMPLEMENTED`、`DEFERRED_WITH_REASON` 或 `BLOCKED_EXTERNAL`。

## 3. 建议的纵向实施顺序

1. **Admin 纵向切片**：`P05-022`, `P05-023`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
2. **Android 纵向切片**：`P05-001`, `P05-002`, `P05-007`, `P05-013`, `P05-014`, `P05-015`, `P05-016`, `P05-017`, `P05-018`, `P05-019`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
3. **Backend 纵向切片**：`P05-003`, `P05-020`, `P05-024`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
4. **Scheduler 纵向切片**：`P05-021`, `P05-025`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
5. **Security 纵向切片**：`P05-004`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
6. **Worker 纵向切片**：`P05-005`, `P05-006`, `P05-008`, `P05-009`, `P05-010`, `P05-011`, `P05-012`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。

## 4. Feature 逐项实施合同

### P05-001 — 请求 R2 预签名上传凭据

- **产品表面/页面或菜单**：`Android` / `附件选择器`。
- **用户或管理员动作**：请求 R2 预签名上传凭据。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/files/uploads`；负责服务为 `File Service`。
- **数据实体**：`attachments|upload_sessions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“附件选择器”完成“请求 R2 预签名上传凭据”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-001-UNIT|P05-001-API|P05-001-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-002 — 直传文件并显示进度

- **产品表面/页面或菜单**：`Android` / `上传进度`。
- **用户或管理员动作**：直传文件并显示进度。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `r2://presigned-upload`；负责服务为 `R2`。
- **数据实体**：`attachments|upload_sessions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/上传记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“上传进度”完成“直传文件并显示进度”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/上传记录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-002-UNIT|P05-002-API|P05-002-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-003 — 完成上传并校验对象元数据

- **产品表面/页面或菜单**：`Backend` / `上传完成`。
- **用户或管理员动作**：完成上传并校验对象元数据。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/files/uploads/{uploadId}/complete`；负责服务为 `File Service`。
- **数据实体**：`attachments|file_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“上传完成”完成“完成上传并校验对象元数据”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-003-UNIT|P05-003-API|P05-003-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-004 — 校验 MIME、扩展名、大小和哈希

- **产品表面/页面或菜单**：`Security` / `文件接收`。
- **用户或管理员动作**：校验 MIME、扩展名、大小和哈希。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://file-validation`；负责服务为 `File Security`。
- **数据实体**：`attachments|security_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/安全策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件接收”完成“校验 MIME、扩展名、大小和哈希”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/安全策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-004-UNIT|P05-004-API|P05-004-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-005 — 执行恶意文件扫描和隔离

- **产品表面/页面或菜单**：`Worker` / `文件扫描`。
- **用户或管理员动作**：执行恶意文件扫描和隔离。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://file-security-scan`；负责服务为 `File Worker`。
- **数据实体**：`jobs|file_scan_results`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/隔离区`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`file_security_scan`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件扫描”完成“执行恶意文件扫描和隔离”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/隔离区”能够查询或控制该能力，变更写入审计日志；异步任务 file_security_scan 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-005-UNIT|P05-005-API|P05-005-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-006 — 生成图片缩略图和基本元数据

- **产品表面/页面或菜单**：`Worker` / `图片处理`。
- **用户或管理员动作**：生成图片缩略图和基本元数据。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://image-thumbnail`；负责服务为 `File Worker`。
- **数据实体**：`artifact_assets|file_extractions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/处理任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`thumbnail_generation`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“图片处理”完成“生成图片缩略图和基本元数据”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/处理任务”能够查询或控制该能力，变更写入审计日志；异步任务 thumbnail_generation 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-006-UNIT|P05-006-API|P05-006-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-007 — 将图片作为视觉输入发送给模型

- **产品表面/页面或菜单**：`Android` / `会话/图片附件`。
- **用户或管理员动作**：将图片作为视觉输入发送给模型。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/conversations/{conversationId}/runs`；负责服务为 `AI Runtime`。
- **数据实体**：`attachments|message_parts|message_runs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`模型与供应商/能力`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“会话/图片附件”完成“将图片作为视觉输入发送给模型”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“模型与供应商/能力”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P05-007-UNIT|P05-007-API|P05-007-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-008 — 解析 PDF 文本、页面和预览

- **产品表面/页面或菜单**：`Worker` / `PDF解析`。
- **用户或管理员动作**：解析 PDF 文本、页面和预览。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://parse-pdf`；负责服务为 `Document Worker`。
- **数据实体**：`file_extractions|document_pages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`parse_pdf`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“PDF解析”完成“解析 PDF 文本、页面和预览”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 parse_pdf 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-008-UNIT|P05-008-API|P05-008-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-009 — 解析 DOCX 文本和结构

- **产品表面/页面或菜单**：`Worker` / `DOCX解析`。
- **用户或管理员动作**：解析 DOCX 文本和结构。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://parse-docx`；负责服务为 `Document Worker`。
- **数据实体**：`file_extractions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`parse_docx`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“DOCX解析”完成“解析 DOCX 文本和结构”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 parse_docx 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-009-UNIT|P05-009-API|P05-009-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-010 — 解析 PPTX 页面文本和预览

- **产品表面/页面或菜单**：`Worker` / `PPTX解析`。
- **用户或管理员动作**：解析 PPTX 页面文本和预览。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://parse-pptx`；负责服务为 `Document Worker`。
- **数据实体**：`file_extractions|document_pages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`parse_pptx`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“PPTX解析”完成“解析 PPTX 页面文本和预览”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 parse_pptx 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-010-UNIT|P05-010-API|P05-010-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-011 — 解析 XLSX 工作表和单元格结构

- **产品表面/页面或菜单**：`Worker` / `XLSX解析`。
- **用户或管理员动作**：解析 XLSX 工作表和单元格结构。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://parse-xlsx`；负责服务为 `Document Worker`。
- **数据实体**：`file_extractions|spreadsheet_sheets`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`parse_xlsx`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“XLSX解析”完成“解析 XLSX 工作表和单元格结构”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 parse_xlsx 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-011-UNIT|P05-011-API|P05-011-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-012 — 解析纯文本、Markdown和代码文件

- **产品表面/页面或菜单**：`Worker` / `文本解析`。
- **用户或管理员动作**：解析纯文本、Markdown和代码文件。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://parse-text`；负责服务为 `Document Worker`。
- **数据实体**：`file_extractions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`parse_text`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文本解析”完成“解析纯文本、Markdown和代码文件”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 parse_text 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-012-UNIT|P05-012-API|P05-012-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-013 — 展示附件上传、扫描、解析和失败状态

- **产品表面/页面或菜单**：`Android` / `附件卡片`。
- **用户或管理员动作**：展示附件上传、扫描、解析和失败状态。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/files/{fileId}`；负责服务为 `File Service`。
- **数据实体**：`attachments|jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“附件卡片”完成“展示附件上传、扫描、解析和失败状态”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-013-UNIT|P05-013-API|P05-013-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-014 — 将资料库文件添加到消息

- **产品表面/页面或菜单**：`Android` / `资料库选择器`。
- **用户或管理员动作**：将资料库文件添加到消息。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/mobile/v1/conversations/{conversationId}/attachments`；负责服务为 `Conversation Service`。
- **数据实体**：`message_parts|attachments`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`对话与内容/附件`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“资料库选择器”完成“将资料库文件添加到消息”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“对话与内容/附件”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-014-UNIT|P05-014-API|P05-014-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-015 — 分页展示个人文件资料库

- **产品表面/页面或菜单**：`Android` / `工作/文件`。
- **用户或管理员动作**：分页展示个人文件资料库。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/files`；负责服务为 `File Service`。
- **数据实体**：`attachments|file_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“工作/文件”完成“分页展示个人文件资料库”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-015-UNIT|P05-015-API|P05-015-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-016 — 按名称、类型和时间搜索文件

- **产品表面/页面或菜单**：`Android` / `文件搜索`。
- **用户或管理员动作**：按名称、类型和时间搜索文件。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/files/search`；负责服务为 `Search Service`。
- **数据实体**：`attachments`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件搜索”完成“按名称、类型和时间搜索文件”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-016-UNIT|P05-016-API|P05-016-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-017 — 重命名文件

- **产品表面/页面或菜单**：`Android` / `文件菜单`。
- **用户或管理员动作**：重命名文件。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PATCH` `/api/mobile/v1/files/{fileId}`；负责服务为 `File Service`。
- **数据实体**：`attachments`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件详情`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件菜单”完成“重命名文件”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件详情”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-017-UNIT|P05-017-API|P05-017-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-018 — 下载原始文件或派生文件

- **产品表面/页面或菜单**：`Android` / `文件详情`。
- **用户或管理员动作**：下载原始文件或派生文件。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/mobile/v1/files/{fileId}/download`；负责服务为 `Download Service`。
- **数据实体**：`attachments|download_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/下载记录`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件详情”完成“下载原始文件或派生文件”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/下载记录”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-018-UNIT|P05-018-API|P05-018-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-019 — 删除文件并处理引用关系

- **产品表面/页面或菜单**：`Android` / `文件菜单`。
- **用户或管理员动作**：删除文件并处理引用关系。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`DELETE` `/api/mobile/v1/files/{fileId}`；负责服务为 `File Service`。
- **数据实体**：`attachments|message_parts|project_files`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/删除策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件菜单”完成“删除文件并处理引用关系”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/删除策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-019-UNIT|P05-019-API|P05-019-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-020 — 按套餐执行文件大小和空间配额

- **产品表面/页面或菜单**：`Backend` / `文件配额`。
- **用户或管理员动作**：按套餐执行文件大小和空间配额。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://file-quota`；负责服务为 `Entitlement Service`。
- **数据实体**：`quota_counters|entitlement_grants`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`商业化/权益配置`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件配额”完成“按套餐执行文件大小和空间配额”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“商业化/权益配置”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-020-UNIT|P05-020-API|P05-020-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-021 — 生成过期上传会话和孤儿对象清理任务

- **产品表面/页面或菜单**：`Scheduler` / `文件清理`。
- **用户或管理员动作**：生成过期上传会话和孤儿对象清理任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://storage-cleanup`；负责服务为 `File Worker`。
- **数据实体**：`upload_sessions|attachments`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/清理任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`system`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`storage_cleanup`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件清理”完成“生成过期上传会话和孤儿对象清理任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/清理任务”能够查询或控制该能力，变更写入审计日志；异步任务 storage_cleanup 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-021-UNIT|P05-021-API|P05-021-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-022 — 后台查询文件、归属和处理状态

- **产品表面/页面或菜单**：`Admin` / `文件与存储/文件列表`。
- **用户或管理员动作**：后台查询文件、归属和处理状态。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/admin/v1/files`；负责服务为 `File Service`。
- **数据实体**：`attachments|jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/文件列表`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件与存储/文件列表”完成“后台查询文件、归属和处理状态”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/文件列表”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-022-UNIT|P05-022-API|P05-022-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-023 — 后台重试文件解析任务

- **产品表面/页面或菜单**：`Admin` / `文件与存储/解析任务`。
- **用户或管理员动作**：后台重试文件解析任务。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/files/{fileId}/reprocess`；负责服务为 `Job Service`。
- **数据实体**：`jobs|job_attempts`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/解析任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`admin`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`document_reprocess`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“文件与存储/解析任务”完成“后台重试文件解析任务”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/解析任务”能够查询或控制该能力，变更写入审计日志；异步任务 document_reprocess 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-023-UNIT|P05-023-API|P05-023-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-024 — 为模型生成短期安全下载地址

- **产品表面/页面或菜单**：`Backend` / `模型附件适配`。
- **用户或管理员动作**：为模型生成短期安全下载地址。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/internal/v1/files/{fileId}/provider-access`；负责服务为 `File Service`。
- **数据实体**：`attachment_provider_refs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/供应商引用`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型附件适配”完成“为模型生成短期安全下载地址”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/供应商引用”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P05-024-UNIT|P05-024-API|P05-024-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P05-025 — 清理过期上游文件引用

- **产品表面/页面或菜单**：`Scheduler` / `供应商文件引用`。
- **用户或管理员动作**：清理过期上游文件引用。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://provider-file-cleanup`；负责服务为 `File Worker`。
- **数据实体**：`attachment_provider_refs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`文件与存储/清理任务`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`system`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`provider_file_cleanup`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“供应商文件引用”完成“清理过期上游文件引用”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“文件与存储/清理任务”能够查询或控制该能力，变更写入审计日志；异步任务 provider_file_cleanup 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P05-025-UNIT|P05-025-API|P05-025-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

## 5. 阶段测试与发布

1. 运行 `.\ylven.ps1 py scripts/07_VALIDATE_CONTRACTS.py --phase P05`。
2. 开发期间只运行受影响模块测试；阶段末运行 Go、Web、Android、迁移和契约必要全量测试。
3. 将 `status/P05_FEATURE_STATUS.yaml` 中每个条目更新为最终状态并附证据或原因。
4. 执行 `.\scripts\05_RELEASE_PHASE.ps1 -Phase P05`，再执行 `.\scripts\06_VERIFY_RELEASE.ps1 -Phase P05`。
5. 项目所有者安装 APK、体验当前后台菜单并填写 OWNER_ACCEPTANCE.md；Codex在收到批准前停止，不推进下一阶段。

## 6. 阶段不通过的典型情形

- APK 只是 Shell，Feature Map 中声称的链路没有真实后端或持久化。
- 测试报告没有命令、退出码和日志，只写“已检查”。
- App 直接保存 Sub2API、R2、邮件、Turnstile Secret 或管理 Token。
- 页面、接口、数据库和管理后台使用不同字段含义，或模型能力被写死在 APK。
- 真实外部联调缺失却被标记 IMPLEMENTED，或者非关键外部配置阻塞整个阶段。

<!-- YLVEN_UI_CONTRACT_V1_2 -->

## UI-LOCK：本阶段固定视觉合同（YL-DS-1.2.0）

> 本节是本阶段强制合同，不是参考建议。每次开启新 Codex 会话、上下文压缩后恢复或开始本阶段内任一小版本时，都必须重新读取本节及对应 Work Packet。

### 固定数值

| 固定项 | 数值/规则 |
|---|---|
| 设计系统版本 | `YL-DS-1.2.0` |
| Android 基准画布 | `360×800dp`，效果图 `1080×2400px @3x`，不带手机外框，包含系统栏参考区 |
| 页面左右边距 | 普通页 `16dp`；登录注册页 `24dp` |
| 顶部栏 / 底部导航 | `56dp` / `64dp` 内容高度 |
| 主按钮 | 高 `52dp`，圆角 `14dp`，左右内边距 `20dp` |
| 输入框 | 最小高 `52dp`，圆角 `14dp`，左右内边距 `16dp` |
| 卡片 | 圆角 `16dp`，内边距 `16dp`，边框 `1dp` |
| 间距 | 行内 `8dp`；卡片 `12dp`；区块 `24dp` |
| 字体 | 页标题 `24/32sp 700`；区块 `20/28sp 700`；卡片 `17/24sp 600`；正文 `16/24sp`；次正文 `14/20sp`；说明 `12/16sp` |
| 图标 / 触控 | 常规图标 `24dp`；最小触控区 `48dp` |
| 聊天输入框 | 高 `52–148dp`，圆角 `24dp` |
| 用户消息 | 最大宽度 `84%`，圆角 `18dp`；AI 回答为全宽无大气泡布局 |
| Bottom Sheet / Dialog | 顶部圆角 `28dp` / 圆角 `20dp`、最大宽 `312dp` |
| 浅色主配色 | 背景 `#F6F7FB`；卡片 `#FFFFFF`；主色 `#5B61F6`；主文字 `#101828`；次文字 `#475467`；边框 `#E4E7EC` |
| 管理后台 | 画布 `1440px`；侧栏 `248px`；顶栏 `64px`；内容边距 `24px`；表格行 `48px` |
| 开发者中心 | 画布 `1440px`；侧栏 `240px`；顶栏 `64px`；内容边距 `32px` |

### 本阶段小版本

- [P05-W01 — 附件选择、上传、安全校验与对象存储](../work-packets/P05-W01.md)
- [P05-W02 — 图片识别、文档解析与供应商文件引用](../work-packets/P05-W02.md)
- [P05-W03 — 文件库、搜索、详情、删除与资料选择](../work-packets/P05-W03.md)
- [P05-W04 — 后台文件治理、生命周期、隔离区与清理](../work-packets/P05-W04.md)

### 本阶段页面与状态效果图

- `YL-A-044` — 附件选择器（ANDROID；7 状态；P05-W01）
- `YL-A-045` — 上传进度（ANDROID；9 状态；P05-W01）
- `YL-A-046` — 会话/图片附件（ANDROID；9 状态；P05-W01）
- `YL-A-047` — 附件卡片（ANDROID；9 状态；P05-W02）
- `YL-A-048` — 资料库选择器（ANDROID；7 状态；P05-W03）
- `YL-A-049` — 工作/文件（ANDROID；9 状态；P05-W03）
- `YL-A-050` — 文件搜索（ANDROID；9 状态；P05-W03）
- `YL-A-051` — 文件菜单（ANDROID；5 状态；P05-W03）
- `YL-A-052` — 文件详情（ANDROID；9 状态；P05-W03）
- `YL-M-070` — 模型与供应商/能力（ADMIN；11 状态；P04-W04, P05-W01）
- `YL-M-076` — 文件与存储/文件列表（ADMIN；8 状态；P05-W01, P05-W03, P05-W04, P07-W01）
- `YL-M-077` — 文件与存储/上传记录（ADMIN；8 状态；P05-W01）
- `YL-M-078` — 文件与存储/安全策略（ADMIN；11 状态；P05-W01）
- `YL-M-079` — 文件与存储/隔离区（ADMIN；8 状态；P05-W01）
- `YL-M-080` — 文件与存储/处理任务（ADMIN；8 状态；P05-W01）
- `YL-M-081` — 文件与存储/解析任务（ADMIN；8 状态；P05-W02, P05-W04）
- `YL-M-082` — 文件与存储/文件详情（ADMIN；11 状态；P05-W02, P05-W03）
- `YL-M-083` — 对话与内容/附件（ADMIN；8 状态；P05-W03）
- `YL-M-084` — 文件与存储/下载记录（ADMIN；8 状态；P05-W03）
- `YL-M-085` — 文件与存储/删除策略（ADMIN；11 状态；P05-W03）
- `YL-M-086` — 商业化/权益配置（ADMIN；11 状态；P05-W04, P06-W04）
- `YL-M-087` — 文件与存储/清理任务（ADMIN；8 状态；P05-W04）
- `YL-M-088` — 文件与存储/供应商引用（ADMIN；8 状态；P05-W04）

本阶段共绑定 **23 个页面、200 个状态效果图**。具体 State ID、图片路径、批准状态与 SHA-256 必须读取：

- `contracts/ui-state-catalog.csv`
- `contracts/mockup-manifest.csv`
- `ui/pages/<surface>/<PageID>.yaml`

### 强制防漂移规则

1. 页面禁止自行写入未登记的字号、圆角、颜色、间距、高度和阴影；只能调用设计 Token 和公共组件。
2. 页面和每个适用状态都必须有独立 Page/State ID；不能用一张成功态效果图推测所有异常状态。
3. 效果图未批准前不得完成视觉实现；错误、离线、超时、空态、成功提示和操作失败不得由 Codex自由设计。
4. 效果图只约束视觉；功能只依据 Feature ID。虚拟样例文字和按钮不构成需求。
5. 阶段发布必须包含 `UI_SCREENSHOT_INDEX.csv`、`VISUAL_DIFF_REPORT.md` 与 `UI_CONTRACT_REPORT.json`。
6. 真机截图与批准效果图存在未解释差异时，本阶段不得标记完成。

<!-- YLVEN_V1_6_CONTINUITY_RULES -->
## V1.6 强制版本连续性、关闭记录与执行环境规则

1. **新会话先恢复，不猜版本。** 新 Codex 会话、上下文压缩恢复、切换分支或 `git pull` 后，第一条命令固定为 `.\ylven.ps1 resume`。只能开发控制器输出的 `CURRENT_PHASE.yaml` 与 `CURRENT_WORK_PACKET.yaml`；聊天中的“继续下个版本”没有状态效力。
2. **定义与状态严格分离。** `contracts/work-packet-map.yaml` 只定义范围；`status/WORK_PACKET_STATUS.yaml` 是唯一可变工作包状态表；禁止再创建第二份当前阶段、当前工作包、状态表、历史或版本总账。
3. **代码写完不等于工作包关闭。** 当前包绑定的 Feature 必须具有最终状态、原因/证据和真实测试记录，代码与测试必须形成干净的实现 Commit，然后执行 `.\ylven.ps1 close-packet -Packet <当前WorkPacketID>`。控制器追加哈希链 `WORK_PACKET_HISTORY.jsonl`、更新运行状态、创建状态 Commit、尝试推送并机械选择下一包。
4. **外部原因延期必须留痕。** 只有真实外部依赖才可执行 `.\ylven.ps1 defer-packet -Packet <当前WorkPacketID> -Reason <具体原因>`；不得用延期掩盖未实现代码、未运行测试或缺失证据。
5. **阶段完成不等于正式版本关闭。** 全部工作包最终化后，阶段只能进入 `READY_FOR_RELEASE`。先执行 `.\ylven.ps1 release -Phase <当前阶段>`，让 GitHub Actions 对精确 Commit 构建、模拟器点击测试、升级安装、截图和视觉回归，并交付同一 CI Artifact。
6. **项目所有者批准后才关闭正式版本。** `OWNER_ACCEPTANCE.md` 含 `- Result: APPROVED`，且版本、Commit、CI Run、Artifact、APK SHA-256 一致后，执行 `.\ylven.ps1 close-release -Phase <当前阶段>`。控制器创建/校验不可变 Tag、追加哈希链 `RELEASE_LEDGER.jsonl`、创建状态 Commit并推进到相邻阶段第一个未完成包。
7. **仓库公开是项目事实，不是阻塞。** 当前仓库由项目所有者确认为 Public。每次提交、推送和发布前必须运行公开仓库 Secret 门禁；服务器地址、SSH 信息、密钥、签名材料、管理员一次性密码、Cloudflare/数据库/API 凭据只能放在本地忽略目录、GitHub Environment 或服务器秘密存储中。
8. **首次空仓库是正常初始化场景。** 缺少 `.git`、远端为空、没有默认分支时运行 `.\ylven.ps1 bootstrap`；Bootstrap 自动初始化 `main`、绑定唯一 `origin`、完成安全扫描并尝试首次推送。推送鉴权失败只阻塞远程 CI，不阻塞本地合同和代码工作。
9. **本机是轻量控制端。** 本机只要求 PowerShell、Git、uv、Spec Kit、OpenSSH，发布下载时使用 GitHub CLI；缺少 Java、Go、Gradle、ADB、Android SDK、Docker、Node 或系统 Python不得列为业务阻塞。构建、模拟器和视觉回归在 GitHub Actions；Docker、数据库和 staging 集成在已连接线上服务器执行。
10. **本机 Git 推送失败时的固定备用路径。** 保持同一 Commit SHA、阶段分支和 `origin` 不变，通过已验证的 SSH/SFTP 将完整 Git Bundle 传到服务器，校验 SHA-256 与 `git bundle verify` 后由服务器推送 GitHub；不得把 Token、密钥或 Secret 放入仓库、Bundle、命令行或日志，推送后必须回读 GitHub 分支 SHA。
10. **快速执行优先。** 每个工作包只做一次必要预检，随后直接编码并运行受影响测试；输入未变化时禁止重复全仓库审计。产出优先级固定为：可运行代码 → 自动测试 → 必需合同 → 必需说明。
11. **每版交付必须可追溯。** 桌面交付只能来自通过 CI 的精确 Artifact，并包含计划功能、完成功能、自动测试、项目所有者测试清单、截图索引、视觉差异、端点与 DNS 状态、Build Info、CI Provenance、已知问题和 SHA-256。
