# P11 开发者中心、公共 API、API Key 与 SSO

> 本阶段包含 **32 个 Feature ID**。本文件与 `contracts/feature-map.yaml` 一致；表中任何条目都不能仅以页面占位、静态返回或聊天声明视为完成。

## 1. 阶段目标

面向开发者提供 YLVEN API、OpenAI 兼容入口、Key、预算、日志、文档、Playground 和 OIDC/Sub2API 登录。

## 2. 阶段边界

- 只实现本文件列出的 Feature ID；下一阶段内容不得为了“顺手”提前改变数据契约。
- 每个可见功能必须同时完成必要的 Android/网页状态、后端接口或内部事件、持久化、管理后台控制、权限、日志指标和测试。
- 外部凭据缺失时完成 Adapter、契约 mock、feature flag 和 Owner Actions；仅真实联调条目标记 `BLOCKED_EXTERNAL`。
- 不使用永久假数据、固定余额、固定模型列表、静态成功响应或不可追踪占位作品。
- 阶段发布前，所有本阶段 Feature 状态必须是 `IMPLEMENTED`、`DEFERRED_WITH_REASON` 或 `BLOCKED_EXTERNAL`。

## 3. 建议的纵向实施顺序

1. **Admin 纵向切片**：`P11-031`, `P11-032`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
2. **Backend 纵向切片**：`P11-011`, `P11-012`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
3. **Developer Web 纵向切片**：`P11-001`, `P11-002`, `P11-003`, `P11-004`, `P11-005`, `P11-006`, `P11-007`, `P11-008`, `P11-009`, `P11-010`, `P11-022`, `P11-023`, `P11-024`, `P11-025`, `P11-026`, `P11-027`, `P11-028`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
4. **Public API 纵向切片**：`P11-013`, `P11-014`, `P11-015`, `P11-016`, `P11-017`, `P11-018`, `P11-019`, `P11-020`, `P11-021`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
5. **Security 纵向切片**：`P11-030`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。
6. **Worker 纵向切片**：`P11-029`。先完成最小可运行链路，再补异常状态、后台控制、观测和测试。

## 4. Feature 逐项实施合同

### P11-001 — 展示开发者中心概览

- **产品表面/页面或菜单**：`Developer Web` / `开发者概览`。
- **用户或管理员动作**：展示开发者中心概览。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/developer/v1/overview`；负责服务为 `Developer Service`。
- **数据实体**：`api_keys|usage_aggregates|balance_projections`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/概览`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“开发者概览”完成“展示开发者中心概览”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/概览”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-001-UNIT|P11-001-API|P11-001-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-002 — 激活开发者功能并同意 API 条款

- **产品表面/页面或菜单**：`Developer Web` / `开发者开通`。
- **用户或管理员动作**：激活开发者功能并同意 API 条款。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/developer/v1/enrollment`；负责服务为 `Developer Service`。
- **数据实体**：`developer_profiles`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/开发者用户`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“开发者开通”完成“激活开发者功能并同意 API 条款”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/开发者用户”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-002-UNIT|P11-002-API|P11-002-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-003 — 使用 YLVEN OIDC 单点登录 Sub2API

- **产品表面/页面或菜单**：`Developer Web` / `Sub2API登录`。
- **用户或管理员动作**：使用 YLVEN OIDC 单点登录 Sub2API。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/oauth2/authorize`；负责服务为 `OIDC Provider`。
- **数据实体**：`oauth_clients|oauth_authorizations|gateway_user_links`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`Sub2API与通道/OIDC`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Sub2API登录”完成“使用 YLVEN OIDC 单点登录 Sub2API”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“Sub2API与通道/OIDC”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-003-UNIT|P11-003-API|P11-003-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-004 — 创建 YLVEN 开发者 API Key

- **产品表面/页面或菜单**：`Developer Web` / `API Key`。
- **用户或管理员动作**：创建 YLVEN 开发者 API Key。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/developer/v1/api-keys`；负责服务为 `API Key Service`。
- **数据实体**：`api_keys|api_key_policies`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key”完成“创建 YLVEN 开发者 API Key”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-004-UNIT|P11-004-API|P11-004-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-005 — 仅创建时显示完整 API Key 一次

- **产品表面/页面或菜单**：`Developer Web` / `API Key创建结果`。
- **用户或管理员动作**：仅创建时显示完整 API Key 一次。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://api-key-reveal-once`；负责服务为 `API Key Service`。
- **数据实体**：`api_keys`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key创建结果”完成“仅创建时显示完整 API Key 一次”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-005-UNIT|P11-005-API|P11-005-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-006 — 分页查询 API Key 和最近使用状态

- **产品表面/页面或菜单**：`Developer Web` / `API Key`。
- **用户或管理员动作**：分页查询 API Key 和最近使用状态。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/developer/v1/api-keys`；负责服务为 `API Key Service`。
- **数据实体**：`api_keys|api_request_logs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key”完成“分页查询 API Key 和最近使用状态”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-006-UNIT|P11-006-API|P11-006-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-007 — 轮换、禁用和删除 API Key

- **产品表面/页面或菜单**：`Developer Web` / `API Key详情`。
- **用户或管理员动作**：轮换、禁用和删除 API Key。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/developer/v1/api-keys/{keyId}/actions`；负责服务为 `API Key Service`。
- **数据实体**：`api_keys|audit_logs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key详情”完成“轮换、禁用和删除 API Key”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-007-UNIT|P11-007-API|P11-007-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-008 — 配置单 Key 总额和月度预算

- **产品表面/页面或菜单**：`Developer Web` / `API Key预算`。
- **用户或管理员动作**：配置单 Key 总额和月度预算。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/developer/v1/api-keys/{keyId}/budget`；负责服务为 `Quota Service`。
- **数据实体**：`api_key_policies|quota_counters`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/预算`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key预算”完成“配置单 Key 总额和月度预算”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/预算”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-008-UNIT|P11-008-API|P11-008-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-009 — 配置 API 账号总预算

- **产品表面/页面或菜单**：`Developer Web` / `预算与告警`。
- **用户或管理员动作**：配置 API 账号总预算。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/developer/v1/budget`；负责服务为 `Quota Service`。
- **数据实体**：`developer_budgets`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/预算`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“预算与告警”完成“配置 API 账号总预算”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/预算”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-009-UNIT|P11-009-API|P11-009-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-010 — 配置 IP 白名单和模型白名单

- **产品表面/页面或菜单**：`Developer Web` / `API Key安全`。
- **用户或管理员动作**：配置 IP 白名单和模型白名单。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/developer/v1/api-keys/{keyId}/policy`；负责服务为 `API Key Service`。
- **数据实体**：`api_key_policies|ip_allowlists`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key安全”完成“配置 IP 白名单和模型白名单”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-010-UNIT|P11-010-API|P11-010-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-011 — Developer Gateway 鉴权和密钥哈希查找

- **产品表面/页面或菜单**：`Backend` / `公共 API 鉴权`。
- **用户或管理员动作**：Developer Gateway 鉴权和密钥哈希查找。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `gateway://authenticate`；负责服务为 `Developer Gateway`。
- **数据实体**：`api_keys`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/安全事件`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“公共 API 鉴权”完成“Developer Gateway 鉴权和密钥哈希查找”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/安全事件”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-011-UNIT|P11-011-API|P11-011-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-012 — 执行用户、Key、模型三级限流

- **产品表面/页面或菜单**：`Backend` / `公共 API 限流`。
- **用户或管理员动作**：执行用户、Key、模型三级限流。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `gateway://rate-limit`；负责服务为 `Developer Gateway`。
- **数据实体**：`quota_counters|rate_limit_counters`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/限流策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“公共 API 限流”完成“执行用户、Key、模型三级限流”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/限流策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-012-UNIT|P11-012-API|P11-012-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-013 — 提供 OpenAI 兼容模型列表

- **产品表面/页面或菜单**：`Public API` / `Models API`。
- **用户或管理员动作**：提供 OpenAI 兼容模型列表。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/v1/models`；负责服务为 `Developer Gateway`。
- **数据实体**：`models|model_aliases`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Models API”完成“提供 OpenAI 兼容模型列表”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API版本”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-013-UNIT|P11-013-API|P11-013-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-014 — 提供 OpenAI 兼容 Chat Completions

- **产品表面/页面或菜单**：`Public API` / `Chat API`。
- **用户或管理员动作**：提供 OpenAI 兼容 Chat Completions。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/v1/chat/completions`；负责服务为 `Developer Gateway`。
- **数据实体**：`api_request_logs|usage_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API日志`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Chat API”完成“提供 OpenAI 兼容 Chat Completions”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API日志”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-014-UNIT|P11-014-API|P11-014-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-015 — 提供 OpenAI 兼容 Responses API

- **产品表面/页面或菜单**：`Public API` / `Responses API`。
- **用户或管理员动作**：提供 OpenAI 兼容 Responses API。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/v1/responses`；负责服务为 `Developer Gateway`。
- **数据实体**：`api_request_logs|usage_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API日志`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Responses API”完成“提供 OpenAI 兼容 Responses API”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API日志”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-015-UNIT|P11-015-API|P11-015-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-016 — 提供流式公共 API 响应

- **产品表面/页面或菜单**：`Public API` / `Streaming API`。
- **用户或管理员动作**：提供流式公共 API 响应。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/v1/responses?stream=true`；负责服务为 `Developer Gateway`。
- **数据实体**：`api_request_logs|run_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API日志`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Streaming API”完成“提供流式公共 API 响应”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API日志”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-016-UNIT|P11-016-API|P11-016-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-017 — 提供图片生成公共 API

- **产品表面/页面或菜单**：`Public API` / `Images API`。
- **用户或管理员动作**：提供图片生成公共 API。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/v1/images/generations`；负责服务为 `Developer Gateway`。
- **数据实体**：`jobs|api_request_logs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API日志`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`image_generation`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Images API”完成“提供图片生成公共 API”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API日志”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账；异步任务 image_generation 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P11-017-UNIT|P11-017-API|P11-017-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-018 — 提供 YLVEN 原生会话 API

- **产品表面/页面或菜单**：`Public API` / `YLVEN Conversations`。
- **用户或管理员动作**：提供 YLVEN 原生会话 API。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/ylven/v1/conversations`；负责服务为 `Developer Gateway`。
- **数据实体**：`conversations|messages`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“YLVEN Conversations”完成“提供 YLVEN 原生会话 API”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API版本”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-018-UNIT|P11-018-API|P11-018-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-019 — 提供 YLVEN 原生文件和任务 API

- **产品表面/页面或菜单**：`Public API` / `YLVEN Files/Jobs`。
- **用户或管理员动作**：提供 YLVEN 原生文件和任务 API。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/ylven/v1/files`；负责服务为 `Developer Gateway`。
- **数据实体**：`attachments|jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`developer_api_key`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“YLVEN Files/Jobs”完成“提供 YLVEN 原生文件和任务 API”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API版本”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-019-UNIT|P11-019-API|P11-019-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-020 — 使用 YLVEN 稳定模型别名

- **产品表面/页面或菜单**：`Public API` / `Model Aliases`。
- **用户或管理员动作**：使用 YLVEN 稳定模型别名。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/v1/models`；负责服务为 `Model Catalog Service`。
- **数据实体**：`model_aliases`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/模型别名`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Model Aliases”完成“使用 YLVEN 稳定模型别名”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/模型别名”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-020-UNIT|P11-020-API|P11-020-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-021 — 返回稳定、可文档化的错误码

- **产品表面/页面或菜单**：`Public API` / `Errors`。
- **用户或管理员动作**：返回稳定、可文档化的错误码。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `contract://public-api-errors`；负责服务为 `Developer Gateway`。
- **数据实体**：`error_catalog`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/错误码`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Errors”完成“返回稳定、可文档化的错误码”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/错误码”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-021-UNIT|P11-021-API|P11-021-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-022 — 查询按日期、模型、Key和来源的用量

- **产品表面/页面或菜单**：`Developer Web` / `用量统计`。
- **用户或管理员动作**：查询按日期、模型、Key和来源的用量。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/developer/v1/usage`；负责服务为 `Usage Service`。
- **数据实体**：`usage_aggregates`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/用量`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“用量统计”完成“查询按日期、模型、Key和来源的用量”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/用量”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-022-UNIT|P11-022-API|P11-022-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-023 — 查询逐请求 API 日志

- **产品表面/页面或菜单**：`Developer Web` / `调用日志`。
- **用户或管理员动作**：查询逐请求 API 日志。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/developer/v1/request-logs`；负责服务为 `Developer Log Service`。
- **数据实体**：`api_request_logs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API日志`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“调用日志”完成“查询逐请求 API 日志”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API日志”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-023-UNIT|P11-023-API|P11-023-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-024 — 在 Playground 测试模型和参数

- **产品表面/页面或菜单**：`Developer Web` / `API Playground`。
- **用户或管理员动作**：在 Playground 测试模型和参数。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/developer/v1/playground/run`；负责服务为 `Developer Gateway`。
- **数据实体**：`api_request_logs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/Playground`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`usage_metered`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Playground”完成“在 Playground 测试模型和参数”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/Playground”能够查询或控制该能力，变更写入审计日志；计量、额度预检、冻结、结算或回滚不得重复扣费，账本可对账。
- **自动测试 ID**：`P11-024-UNIT|P11-024-API|P11-024-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-025 — 展示动态模型能力和价格文档

- **产品表面/页面或菜单**：`Developer Web` / `模型与价格`。
- **用户或管理员动作**：展示动态模型能力和价格文档。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/api/developer/v1/models`；负责服务为 `Pricing Service`。
- **数据实体**：`models|model_price_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`商业化/模型价格`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“模型与价格”完成“展示动态模型能力和价格文档”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“商业化/模型价格”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-025-UNIT|P11-025-API|P11-025-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-026 — 发布版本化 API 文档和代码示例

- **产品表面/页面或菜单**：`Developer Web` / `API文档`。
- **用户或管理员动作**：发布版本化 API 文档和代码示例。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/docs/api/{version}`；负责服务为 `Documentation Service`。
- **数据实体**：`api_versions|sdk_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API文档`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API文档”完成“发布版本化 API 文档和代码示例”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API文档”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-026-UNIT|P11-026-API|P11-026-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-027 — 配置余额和异常用量告警

- **产品表面/页面或菜单**：`Developer Web` / `预算与告警`。
- **用户或管理员动作**：配置余额和异常用量告警。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`PUT` `/api/developer/v1/alerts`；负责服务为 `Alert Service`。
- **数据实体**：`alert_rules`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/告警`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“预算与告警”完成“配置余额和异常用量告警”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/告警”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-027-UNIT|P11-027-API|P11-027-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-028 — 注册 Webhook 并验证签名

- **产品表面/页面或菜单**：`Developer Web` / `Webhook`。
- **用户或管理员动作**：注册 Webhook 并验证签名。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/api/developer/v1/webhooks`；负责服务为 `Webhook Service`。
- **数据实体**：`webhooks`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/Webhook`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Webhook”完成“注册 Webhook 并验证签名”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/Webhook”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-028-UNIT|P11-028-API|P11-028-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-029 — 失败 Webhook 自动重试和死信

- **产品表面/页面或菜单**：`Worker` / `Webhook投递`。
- **用户或管理员动作**：失败 Webhook 自动重试和死信。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `job://webhook-delivery`；负责服务为 `Webhook Worker`。
- **数据实体**：`webhook_deliveries|dead_letter_jobs`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/Webhook`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`system`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`webhook_delivery`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“Webhook投递”完成“失败 Webhook 自动重试和死信”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/Webhook”能够查询或控制该能力，变更写入审计日志；异步任务 webhook_delivery 支持进度、取消、重试、幂等和失败诊断。
- **自动测试 ID**：`P11-029-UNIT|P11-029-API|P11-029-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-030 — 分离 App 内部调用 Key 与开发者 Key

- **产品表面/页面或菜单**：`Security` / `API Key隔离`。
- **用户或管理员动作**：分离 App 内部调用 Key 与开发者 Key。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`N/A` `policy://key-purpose-isolation`；负责服务为 `API Key Service`。
- **数据实体**：`api_keys|gateway_api_key_links`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API Key策略`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“API Key隔离”完成“分离 App 内部调用 Key 与开发者 Key”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API Key策略”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-030-UNIT|P11-030-API|P11-030-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-031 — 后台查询开发者、Key、预算和风险

- **产品表面/页面或菜单**：`Admin` / `开发者平台/开发者详情`。
- **用户或管理员动作**：后台查询开发者、Key、预算和风险。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`GET` `/admin/v1/developers/{developerId}`；负责服务为 `Developer Service`。
- **数据实体**：`developer_profiles|api_keys|security_events`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/开发者用户`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“开发者平台/开发者详情”完成“后台查询开发者、Key、预算和风险”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/开发者用户”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-031-UNIT|P11-031-API|P11-031-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

### P11-032 — 后台配置公共 API 版本和弃用计划

- **产品表面/页面或菜单**：`Admin` / `开发者平台/API版本`。
- **用户或管理员动作**：后台配置公共 API 版本和弃用计划。
- **必须覆盖的 UI/运行状态**：`idle|loading|success|empty|error`。所有状态均需可复现，错误态提供可恢复操作，不允许无限 Loading。
- **接口或事件合同**：`POST` `/admin/v1/api-versions`；负责服务为 `API Lifecycle Service`。
- **数据实体**：`api_versions`。数据库迁移、唯一约束、索引、所有权字段和删除策略必须与该能力一致。
- **管理后台控制**：`开发者平台/API版本`。可变配置必须可查询、可审计；影响生产行为的配置需要版本和回滚。
- **权限**：`authenticated_user`；跨用户资源访问必须统一返回不可泄露资源存在性的错误。
- **计费规则**：`none`；收费动作必须在任务开始前完成预算校验/冻结，并在成功、失败或取消时结算或解冻。
- **异步任务**：`none`；若非 `none`，必须支持幂等提交、状态查询、超时、取消策略、重试上限和死信处理。
- **验收标准**：用户可在“开发者平台/API版本”完成“后台配置公共 API 版本和弃用计划”，成功结果与服务端持久化状态一致；无权限、参数错误、重复提交、网络超时和服务异常均返回稳定错误码并呈现可恢复状态；管理后台“开发者平台/API版本”能够查询或控制该能力，变更写入审计日志。
- **自动测试 ID**：`P11-032-UNIT|P11-032-API|P11-032-E2E`。测试结论必须落入阶段 TESTS.md，并能追溯到真实命令或日志。
- **实现补充**：遵循本阶段架构文档、OpenAPI、错误码目录和数据权威边界；发现合同冲突时先更新合同并记录 ADR，不能静默偏离。

## 5. 阶段测试与发布

1. 运行 `.\ylven.ps1 py scripts/07_VALIDATE_CONTRACTS.py --phase P11`。
2. 开发期间只运行受影响模块测试；阶段末运行 Go、Web、Android、迁移和契约必要全量测试。
3. 将 `status/P11_FEATURE_STATUS.yaml` 中每个条目更新为最终状态并附证据或原因。
4. 执行 `.\scripts\05_RELEASE_PHASE.ps1 -Phase P11`，再执行 `.\scripts\06_VERIFY_RELEASE.ps1 -Phase P11`。
5. 项目所有者安装 APK、体验当前后台菜单并填写 OWNER_ACCEPTANCE.md；Codex在收到批准前停止，不推进下一阶段。

## 6. 阶段不通过的典型情形

- APK 只是 Shell，Feature Map 中声称的链路没有真实后端或持久化。
- 测试报告没有命令、退出码和日志，只写“已检查”。
- App 直接保存 Sub2API、R2、邮件、Turnstile Secret 或管理 Token。
- 页面、接口、数据库和管理后台使用不同字段含义，或模型能力被写死在 APK。
- 真实外部联调缺失却被标记 IMPLEMENTED，或者非关键外部配置阻塞整个阶段。

<!-- YLVEN_UI_CONTRACT_V1_2 -->

## UI-LOCK：本阶段固定视觉合同（YL-DS-1.2.0）

> 本节是本阶段强制合同，不是参考建议。每次开启新 Codex 会话、上下文压缩后恢复或开始本阶段内任一小版本时，都必须重新读取本节及对应 Work Packet。

### 固定数值

| 固定项 | 数值/规则 |
|---|---|
| 设计系统版本 | `YL-DS-1.2.0` |
| Android 基准画布 | `360×800dp`，效果图 `1080×2400px @3x`，不带手机外框，包含系统栏参考区 |
| 页面左右边距 | 普通页 `16dp`；登录注册页 `24dp` |
| 顶部栏 / 底部导航 | `56dp` / `64dp` 内容高度 |
| 主按钮 | 高 `52dp`，圆角 `14dp`，左右内边距 `20dp` |
| 输入框 | 最小高 `52dp`，圆角 `14dp`，左右内边距 `16dp` |
| 卡片 | 圆角 `16dp`，内边距 `16dp`，边框 `1dp` |
| 间距 | 行内 `8dp`；卡片 `12dp`；区块 `24dp` |
| 字体 | 页标题 `24/32sp 700`；区块 `20/28sp 700`；卡片 `17/24sp 600`；正文 `16/24sp`；次正文 `14/20sp`；说明 `12/16sp` |
| 图标 / 触控 | 常规图标 `24dp`；最小触控区 `48dp` |
| 聊天输入框 | 高 `52–148dp`，圆角 `24dp` |
| 用户消息 | 最大宽度 `84%`，圆角 `18dp`；AI 回答为全宽无大气泡布局 |
| Bottom Sheet / Dialog | 顶部圆角 `28dp` / 圆角 `20dp`、最大宽 `312dp` |
| 浅色主配色 | 背景 `#F6F7FB`；卡片 `#FFFFFF`；主色 `#5B61F6`；主文字 `#101828`；次文字 `#475467`；边框 `#E4E7EC` |
| 管理后台 | 画布 `1440px`；侧栏 `248px`；顶栏 `64px`；内容边距 `24px`；表格行 `48px` |
| 开发者中心 | 画布 `1440px`；侧栏 `240px`；顶栏 `64px`；内容边距 `32px` |

### 本阶段小版本

- [P11-W01 — 开发者开通、YLVEN OIDC 与 Sub2API SSO](../work-packets/P11-W01.md)
- [P11-W02 — API Key 创建、展示一次、预算和安全策略](../work-packets/P11-W02.md)
- [P11-W03 — OpenAI 兼容 API、YLVEN 原生 API 与模型别名](../work-packets/P11-W03.md)
- [P11-W04 — 开发者概览、日志、Playground、价格、文档和 Webhook](../work-packets/P11-W04.md)
- [P11-W05 — 公共 API 安全、限流、版本、后台运营和兼容性](../work-packets/P11-W05.md)

### 本阶段页面与状态效果图

- `YL-M-074` — 商业化/模型价格（ADMIN；8 状态；P04-W05, P10-W03, P10-W05, P11-W04）
- `YL-M-152` — 开发者平台/概览（ADMIN；8 状态；P11-W01）
- `YL-M-153` — 开发者平台/开发者用户（ADMIN；8 状态；P11-W01, P11-W05）
- `YL-M-154` — Sub2API与通道/OIDC（ADMIN；11 状态；P11-W01）
- `YL-M-155` — 开发者平台/API Key（ADMIN；8 状态；P11-W01）
- `YL-M-156` — 开发者平台/预算（ADMIN；8 状态；P11-W02）
- `YL-M-157` — 开发者平台/API Key策略（ADMIN；11 状态；P11-W02, P11-W05）
- `YL-M-158` — 开发者平台/安全事件（ADMIN；8 状态；P11-W02）
- `YL-M-159` — 开发者平台/限流策略（ADMIN；11 状态；P11-W02）
- `YL-M-160` — 开发者平台/API版本（ADMIN；11 状态；P11-W02, P11-W03, P11-W05）
- `YL-M-161` — 开发者平台/API日志（ADMIN；7 状态；P11-W02, P11-W03, P11-W04）
- `YL-M-162` — 开发者平台/模型别名（ADMIN；8 状态；P11-W03）
- `YL-M-163` — 开发者平台/错误码（ADMIN；8 状态；P11-W04）
- `YL-M-164` — 开发者平台/用量（ADMIN；8 状态；P11-W04）
- `YL-M-165` — 开发者平台/Playground（ADMIN；8 状态；P11-W04）
- `YL-M-166` — 开发者平台/API文档（ADMIN；8 状态；P11-W04）
- `YL-M-167` — 开发者平台/告警（ADMIN；7 状态；P11-W05）
- `YL-M-168` — 开发者平台/Webhook（ADMIN；8 状态；P11-W05）
- `YL-D-003` — 开发者概览（DEVELOPER；7 状态；P11-W01）
- `YL-D-004` — 开发者开通（DEVELOPER；10 状态；P11-W01）
- `YL-D-005` — Sub2API登录（DEVELOPER；10 状态；P11-W01）
- `YL-D-006` — API Key（DEVELOPER；10 状态；P11-W01）
- `YL-D-007` — API Key创建结果（DEVELOPER；10 状态；P11-W01）
- `YL-D-008` — API Key详情（DEVELOPER；10 状态；P11-W01）
- `YL-D-009` — API Key预算（DEVELOPER；10 状态；P11-W02）
- `YL-D-010` — 预算与告警（DEVELOPER；10 状态；P11-W02, P11-W05）
- `YL-D-011` — API Key安全（DEVELOPER；10 状态；P11-W02）
- `YL-D-012` — 用量统计（DEVELOPER；10 状态；P11-W04）
- `YL-D-013` — 调用日志（DEVELOPER；7 状态；P11-W04）
- `YL-D-014` — API Playground（DEVELOPER；10 状态；P11-W04）
- `YL-D-015` — 模型与价格（DEVELOPER；10 状态；P11-W04）
- `YL-D-016` — API文档（DEVELOPER；6 状态；P11-W04）
- `YL-D-017` — Webhook（DEVELOPER；7 状态；P11-W05）
- `YL-W-003` — YLVEN OIDC 授权与回调页（WEB；7 状态；P10-W04, P11-W01）

本阶段共绑定 **34 个页面、298 个状态效果图**。具体 State ID、图片路径、批准状态与 SHA-256 必须读取：

- `contracts/ui-state-catalog.csv`
- `contracts/mockup-manifest.csv`
- `ui/pages/<surface>/<PageID>.yaml`

### 强制防漂移规则

1. 页面禁止自行写入未登记的字号、圆角、颜色、间距、高度和阴影；只能调用设计 Token 和公共组件。
2. 页面和每个适用状态都必须有独立 Page/State ID；不能用一张成功态效果图推测所有异常状态。
3. 效果图未批准前不得完成视觉实现；错误、离线、超时、空态、成功提示和操作失败不得由 Codex自由设计。
4. 效果图只约束视觉；功能只依据 Feature ID。虚拟样例文字和按钮不构成需求。
5. 阶段发布必须包含 `UI_SCREENSHOT_INDEX.csv`、`VISUAL_DIFF_REPORT.md` 与 `UI_CONTRACT_REPORT.json`。
6. 真机截图与批准效果图存在未解释差异时，本阶段不得标记完成。

<!-- YLVEN_V1_6_CONTINUITY_RULES -->
## V1.6 强制版本连续性、关闭记录与执行环境规则

1. **新会话先恢复，不猜版本。** 新 Codex 会话、上下文压缩恢复、切换分支或 `git pull` 后，第一条命令固定为 `.\ylven.ps1 resume`。只能开发控制器输出的 `CURRENT_PHASE.yaml` 与 `CURRENT_WORK_PACKET.yaml`；聊天中的“继续下个版本”没有状态效力。
2. **定义与状态严格分离。** `contracts/work-packet-map.yaml` 只定义范围；`status/WORK_PACKET_STATUS.yaml` 是唯一可变工作包状态表；禁止再创建第二份当前阶段、当前工作包、状态表、历史或版本总账。
3. **代码写完不等于工作包关闭。** 当前包绑定的 Feature 必须具有最终状态、原因/证据和真实测试记录，代码与测试必须形成干净的实现 Commit，然后执行 `.\ylven.ps1 close-packet -Packet <当前WorkPacketID>`。控制器追加哈希链 `WORK_PACKET_HISTORY.jsonl`、更新运行状态、创建状态 Commit、尝试推送并机械选择下一包。
4. **外部原因延期必须留痕。** 只有真实外部依赖才可执行 `.\ylven.ps1 defer-packet -Packet <当前WorkPacketID> -Reason <具体原因>`；不得用延期掩盖未实现代码、未运行测试或缺失证据。
5. **阶段完成不等于正式版本关闭。** 全部工作包最终化后，阶段只能进入 `READY_FOR_RELEASE`。先执行 `.\ylven.ps1 release -Phase <当前阶段>`，让 GitHub Actions 对精确 Commit 构建、模拟器点击测试、升级安装、截图和视觉回归，并交付同一 CI Artifact。
6. **项目所有者批准后才关闭正式版本。** `OWNER_ACCEPTANCE.md` 含 `- Result: APPROVED`，且版本、Commit、CI Run、Artifact、APK SHA-256 一致后，执行 `.\ylven.ps1 close-release -Phase <当前阶段>`。控制器创建/校验不可变 Tag、追加哈希链 `RELEASE_LEDGER.jsonl`、创建状态 Commit并推进到相邻阶段第一个未完成包。
7. **仓库公开是项目事实，不是阻塞。** 当前仓库由项目所有者确认为 Public。每次提交、推送和发布前必须运行公开仓库 Secret 门禁；服务器地址、SSH 信息、密钥、签名材料、管理员一次性密码、Cloudflare/数据库/API 凭据只能放在本地忽略目录、GitHub Environment 或服务器秘密存储中。
8. **首次空仓库是正常初始化场景。** 缺少 `.git`、远端为空、没有默认分支时运行 `.\ylven.ps1 bootstrap`；Bootstrap 自动初始化 `main`、绑定唯一 `origin`、完成安全扫描并尝试首次推送。推送鉴权失败只阻塞远程 CI，不阻塞本地合同和代码工作。
9. **本机是轻量控制端。** 本机只要求 PowerShell、Git、uv、Spec Kit、OpenSSH，发布下载时使用 GitHub CLI；缺少 Java、Go、Gradle、ADB、Android SDK、Docker、Node 或系统 Python不得列为业务阻塞。构建、模拟器和视觉回归在 GitHub Actions；Docker、数据库和 staging 集成在已连接线上服务器执行。
10. **本机 Git 推送失败时的固定备用路径。** 保持同一 Commit SHA、阶段分支和 `origin` 不变，通过已验证的 SSH/SFTP 将完整 Git Bundle 传到服务器，校验 SHA-256 与 `git bundle verify` 后由服务器推送 GitHub；不得把 Token、密钥或 Secret 放入仓库、Bundle、命令行或日志，推送后必须回读 GitHub 分支 SHA。
10. **快速执行优先。** 每个工作包只做一次必要预检，随后直接编码并运行受影响测试；输入未变化时禁止重复全仓库审计。产出优先级固定为：可运行代码 → 自动测试 → 必需合同 → 必需说明。
11. **每版交付必须可追溯。** 桌面交付只能来自通过 CI 的精确 Artifact，并包含计划功能、完成功能、自动测试、项目所有者测试清单、截图索引、视觉差异、端点与 DNS 状态、Build Info、CI Provenance、已知问题和 SHA-256。
