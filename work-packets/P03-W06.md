# P03-W06 — 多轮上下文、延迟建会话与自动标题

## 范围

`P03-036`～`P03-045`。建立 Canonical Conversation、Context Compiler、Token 预算、摘要、模型切换重编译、continuation 回退、并发幂等、第一条消息事务、自动标题和手动标题锁定。

## 页面

- `YL-A-020` 会话抽屉；
- `YL-A-023` 会话页；
- `YL-M-061` AI 运行诊断。

## 验收

同一会话连续指代正确；空草稿不入历史；标题在首条有效消息后立即可读并异步优化；切模型连续；长会话压缩不丢关键事实；任何 continuation 失败均自动本地重建。

## 全局正式运营门禁（本包及后续全部阶段）

- 前后端按真实运营实现，不得交付 Demo、Stub、Mock、固定假数据、伪成功接口或仅内存业务状态。
- 普通用户界面不得出现 `streaming`、`connecting`、`provider error`、`request id`、`SSE`、`Sub2API`、`debug`、`demo`、`mock` 等技术/测试文案。
- 页面视觉以本变更单 APPROVED 效果图和固定数值为准；功能以 Feature ID/API/数据合同为准，样例文字不创造功能。
- 当前可以单服务器运行，但会话、流式事件、任务和文件必须满足多实例接管。
- 每个工作包只做一次必要预检，随后直接写代码和受影响测试；禁止重复全仓库分析代替实现。
- 关闭工作包前更新 Feature 证据、测试、页面截图、文案扫描结果和真实提交；P03 未经项目所有者批准不得关闭。

<!-- CO_P03_001_GLOBAL_PRODUCTION_RULES -->
## 正式运营、无 Demo、用户文案与分布式就绪规则

1. 前后端必须按真实运营实现；禁止 Demo、Stub、Mock、静态假数据、伪成功接口和仅内存业务状态。
2. 普通用户界面不得出现 streaming、connecting、provider error、request id、SSE、Sub2API、debug、demo、mock 等技术或测试词；必须映射为普通用户可理解且可恢复的文案。
3. 页面必须使用已批准效果图和固定 UI Token；样例文字不创造功能，功能不得因效果图未展示而删除。
4. 当前可单服务器运行，但 API、AI Runtime、SSE、任务和文件必须满足未来多实例接管；正式状态不得只保存在单节点。
5. 发布前必须通过真实数据库/接口、权限、幂等、迁移、普通用户文案、模拟器交互、截图视觉和 CI Artifact 来源验收。
6. 多写实际代码与测试，少做重复审计；输入未变化时不得循环执行全仓库检查代替实现。

