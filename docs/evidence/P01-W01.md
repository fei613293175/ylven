# P01-W01 Evidence

- Work Packet: `P01-W01`
- Feature IDs: `P01-001` through `P01-006`
- Implementation commit: `689387684315a7a54d2accb16ad7edf90bc8b263`
- CI run: `30992587477`
- CI result: PASS

## Implemented

- `backend/internal/identity/identity.go`: normalized email validation, expiring registration challenges, one-time OTP records, one-time challenge consumption, and PBKDF2-HMAC-SHA256 password hashing.
- `backend/internal/identity/http.go`: API handlers for normalize, registration challenge, Turnstile verification, OTP send/verify and registration completion.
- `backend/cmd/core-api/main.go`: mounts the Identity API alongside the process health endpoints.
- `backend/db/migrations/0002_p01_identity.sql`: auth challenge, email OTP, user and password credential tables with uniqueness and ownership relationships.
- `backend/internal/identity/identity_test.go`: normalization, OTP failure/recovery, one-time consumption and password-hash behavior.

## Verification

GitHub Actions `30992587477` passed `go test ./...`, contract and public-repository safety gates, web shell tests, Android unit/lint/build, emulator acceptance and artifact verification. The local machine has no Go toolchain, so no local Go exit code is claimed.

Turnstile defaults to the external provider boundary and returns `turnstile_unconfigured` without credentials. The test-only `TURNSTILE_MODE=mock` path accepts only the explicit `test-pass` token; no secret is stored in the repository or APK.
