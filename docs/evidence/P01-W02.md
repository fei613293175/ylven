# P01-W02 Evidence

- Work Packet: `P01-W02`
- Feature IDs: `P01-007` through `P01-012`
- Scope: login challenge/OTP, enumeration-safe response, session issue and refresh rotation

## Implementation

- `backend/internal/identity/identity.go`: mutex-protected email uniqueness, login sessions, SHA-256 token digests and one-time refresh rotation.
- `backend/internal/identity/http.go`: `/api/v1/auth/login/challenge`, `/api/v1/auth/login/otp/send`, `/api/v1/auth/login/otp/verify`, `/api/v1/auth/sessions` and `/api/v1/auth/sessions/refresh`.
- `backend/db/migrations/0002_p01_identity.sql`: `sessions` table with unique refresh digest and expiry/consumption fields.
- `backend/internal/identity/identity_test.go`: session issue and refresh-token reuse rejection.

## Verification

The next CI run on the implementation commit must pass `go test ./...`, contract/security gates and the standard Android acceptance workflow before this packet is closed.
