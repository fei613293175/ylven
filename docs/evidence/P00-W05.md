# P00-W05 Evidence

- Work Packet: `P00-W05`
- Scope: `P00-021` through `P00-024`

## Implementation

- Sub2API now has explicit vision/file/image probe results and a `/admin/v1/probes/sub2api/multimodal` endpoint. Missing credentials return `UNCONFIGURED`/HTTP 503; no provider success is claimed without a configured request function.
- `.env.example` contains only `example.invalid` and `secret://` references. `.gitignore` and `scripts/45_VALIDATE_SECRET_BOUNDARY.py` protect local env, signing material, and server secrets.
- `web/admin` is an isolated Vue shell with registered P00 admin routes, permission-denied state, audit/permission control, loading and data panels.
- `web/developer` is a separate Vue shell with stable navigation and loading/populated/empty/error states.

## Tests and gates

- `backend/internal/platform/providers/sub2api/probe_test.go`
- `scripts/45_VALIDATE_SECRET_BOUNDARY.py`
- `web/admin/src/router.test.js`
- `web/developer/src/app.test.js`
- GitHub Actions `web-shell-tests` runs `npm install`, Vitest and Vite build for both apps.
