# P00-W03 Evidence

- Work Packet: `P00-W03`
- Scope: `P00-011` through `P00-015`

## Implementation

- PostgreSQL migration metadata records version/name/checksum/applied time and rejects silent checksum changes; the SQL migration includes uniqueness for schema and idempotency records.
- Cache supports explicit unavailable state, TTL reads, and deterministic fixed-window rate limiting.
- Event bus is a local test adapter with subject delivery and duplicate event IDs rejected; production NATS credentials remain outside the repository.
- Object storage exposes an S3/R2-compatible boundary, while `LocalFS` is the explicitly selected test adapter. The R2 adapter returns `external object storage is not configured` without credentials.
- `contracts/openapi-skeleton.yaml` remains canonical and `scripts/43_VERIFY_OPENAPI_DRIFT.py` checks its recorded SHA-256.

## Tests

| Test | Result |
|---|---|
| database migration tests | Added; checksum immutability and rollback guard |
| cache tests | Added; TTL expiry and rate limit |
| event tests | Added; duplicate suppression |
| storage tests | Added; local read/write and explicit R2 boundary |
| OpenAPI drift check | `scripts/43_VERIFY_OPENAPI_DRIFT.py` |
| Go execution | Delegated to GitHub Actions `backend-process-tests`; local control client has no Go installation |
