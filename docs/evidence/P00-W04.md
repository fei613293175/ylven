# P00-W04 Evidence

- Work Packet: `P00-W04`
- Scope: `P00-016` through `P00-020`

## Implementation

- `scripts/44_VALIDATE_P00_TRACEABILITY.py` requires all 24 P00 Features to carry non-empty status evidence references.
- Telemetry emits structured JSON with request IDs and redacts secret/token/password/API-key fields; metrics exposes Prometheus text and every runtime shell exposes `/internal/metrics`.
- Existing workflow runs contract, public safety, backend, Web/Android build and exact Artifact gates on the exact commit. Existing release and desktop-delivery scripts remain the authoritative release path.
- Sub2API text/stream probe returns `UNCONFIGURED`, `FAILED`, or `SUCCEEDED`; no external credential or success was invented. The admin probe route returns HTTP 503 when unconfigured.

## Tests and gates

- `scripts/44_VALIDATE_P00_TRACEABILITY.py`
- `scripts/07_VALIDATE_CONTRACTS.py`
- `backend/internal/platform/telemetry/telemetry_test.go`
- `backend/internal/platform/providers/sub2api/probe_test.go`
- GitHub Actions `backend-process-tests` executes all Go tests; local Go is intentionally absent.
