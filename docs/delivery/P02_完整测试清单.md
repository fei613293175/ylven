# P02 完整测试清单

- 阶段：P02
- 版本：1.2.0
- 测试结论：后端、合同与视觉门禁 PASS；P02 已改为当前页面内的算式验证。精确 HEAD `7fd3ed6` 的 Android 测试包和 92 张截图已从 run `31136387566` 失败诊断 artifact 取得并交付桌面；该 run 的整体失败仅为视觉基准差异，不能标记为正式 acceptance PASS。最终所有者验收为异步 PENDING。

## 自动化测试

- [x] `scripts/19_VALIDATE_UI_CONTRACTS.py --mode planning`：PASS，332 pages / 2671 states。
- [x] `scripts/20_CHECK_UI_VISUAL_GATE.py --packet P02-W01`：PASS，10 pages。
- [x] `scripts/07_VALIDATE_CONTRACTS.py --phase P02`：PASS，26 features / 1086 tests declared。
- [x] 本地 Go 1.26.0 `go test ./...`：PASS。
- [ ] Android build coordinator：已连续两次在 registry 预检失败；按所有者规则不再重复该路径。
- [ ] GitHub Actions android-emulator-acceptance：run `31136387566` 已完成构建、运行测试和截图，但视觉差异报告失败；新提交推送因两次外部连接错误未到远端，按所有者规则不再重试。
- [x] 精确 HEAD `7fd3ed6` 测试 APK 已交付桌面，SHA-256 `D9B7C91B13DF6F27A112F43E4DAB65934C43B032F9DF458CF1B47BA22E57D4D5`。
- [x] adb install -r 覆盖安装 1.1.0 -> 1.2.0，未清数据，升级与登录态标记保留：PASS。
- [x] staging https://ai-admin.orbexa.cc：`/health`、`/version`、password-policy 实测 200；设备聚合注册/登录/刷新实测设备数为 1。
- [x] 本地后端 first-party 算式验证：答案摘要加盐保存、限时、一次性和五次失败锁定测试 PASS。
- [x] 公网管理员登录与真实用户/会话/安全审计只读核验：PASS。
- [x] `git diff --check`：PASS，只有既有 CRLF 提示。

## Feature 验证矩阵

| Feature ID | UNIT | API | E2E/CI | 所有者实测 | 结果 |
|---|---|---|---|---|---|
| P02-001..P02-006 | PASS | PASS | PENDING (精确 HEAD CI) | PASS | DEV_COMPLETE / CI_PENDING |
| P02-007..P02-011 | PASS | PASS | PENDING (精确 HEAD CI) | PASS | DEV_COMPLETE / CI_PENDING |
| P02-012..P02-016 | PASS | PASS | PENDING (精确 HEAD CI) | PASS | DEV_COMPLETE / CI_PENDING |
| P02-017..P02-021 | PASS | PASS | PENDING (精确 HEAD CI) | PASS | DEV_COMPLETE / CI_PENDING |
| P02-022..P02-026 | PASS | PASS | PENDING (精确 HEAD CI) | PASS | DEV_COMPLETE / CI_PENDING |

## 进入路径、操作步骤、正确预期

- [x] 冷启动已保存会话；正确预期：恢复成功进入账户，过期时只刷新一次，失败回登录：本地实现与测试 PASS；CI pending。
- [ ] 登录输入非法邮箱；正确预期：本地错误且不会发起 challenge。
- [ ] 登录完成安全验证并输入六位 OTP；正确预期：验证码一次消费并进入账户页。
- [ ] 等待倒计时后重发；正确预期：冷却期按钮禁用，服务端拒绝立即重复发送。
- [ ] 注册输入弱密码/不一致确认密码；正确预期：实时策略和确认错误可见且可恢复。
- [ ] 注册安全验证、OTP、完成；正确预期：创建用户、自动会话和个人工作区幂等存在。
- [ ] 退出全部设备；正确预期：服务端会话撤销、Keystore 密文清除、返回登录。
- [ ] 设备页远程撤销另一设备；正确预期：目标会话失效且当前设备不被误撤销。
- [ ] 断网/超时；正确预期：GET 最多一次重试，错误可见且不清敏感会话。
- [ ] 当前页面算式验证取消、错误答案、过期和重复提交；正确预期：弹窗内可恢复且不跳转外部页面。
- [ ] Android 旋转/小屏/字体放大；正确预期：滚动布局不遮挡，控件触控区仍可用。
- [x] 真实公网后台登录并操作当前阶段功能；正确预期：真实数据回读、审计记录可见：PASS（只读与二次确认入口）。

## 失败、恢复和已知限制

- assets.orbexa.cc：BLOCKED_EXTERNAL（未配置）；download.orbexa.cc：BLOCKED_EXTERNAL（未配置 YLVEN 下载服务）。auth.orbexa.cc 已完成 DNS/TLS 与受控验证页公网核验。
- staging 当前仍是旧镜像和 mock 邮件配置；部署 first-party 版本后，算式验证属于基础拦截，不等同于高级风控服务。
- 最终 owner approval：PENDING，必须由用户单独批准后才能 close-release。
