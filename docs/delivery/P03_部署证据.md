# P03 部署证据

- 阶段：P03
- 版本：1.3.0
- Deployment result: NOT_RUN。P03 精确成功 CI Artifact 尚未生成，未执行 staging 或生产部署。
- Health check result: NOT_RUN for P03；2026-08-08 部署前只读探针返回 HTTP 200，但公开 `/version` 仍为 `core-api / 1.2.0-7fd3ed6`，仅证明现有 P02 staging 健康，不是 P03 部署证据。
- Rollback result: NOT_RUN；没有 P03 部署就不存在可验证的 P03 回滚。
- Environment: `obx-test` 隔离 staging；部署前容器 `ylven-p01-api` 正在运行镜像 `ylven-p01-api:7fd3ed6`，仅发布 `127.0.0.1:28201`。
- Pre-deployment compose: `/opt/ylven-p01/p02-compose.yaml`，SHA-256 `21277f82f4ecbc8256b67e7a86554dbca8e58bd2dbf7e15896d797e55b397b55`；当前 `APP_VERSION` 为 `1.2.0-${YLVEN_IMAGE_TAG}`，P03 部署前必须改为 `1.3.0-${YLVEN_IMAGE_TAG}`。
- Source snapshot before CI: GitHub ref `phase/p03-conversations-streaming-chat-and-messages`，发布前不得把该源码快照当作已部署服务。

## 最新精确 CI 状态

- GitHub Actions run：`31208006168`，commit `af9b7bfbd97d3168122568ae520e57e41ecb2a34`。
- 后端、Web、合同、Android 编译、单测与 Lint：PASS。
- Android 模拟器视觉门禁：FAIL；因此只生成 failed diagnostics，没有可交付的 `android-acceptance` Artifact，禁止据此部署或交付 APK。

部署完成后必须补充精确 commit、镜像或 Artifact、公开健康检查、聊天受控失败/成功路径、回滚演练及其时间戳。
