# P03 部署证据

- 阶段：P03
- 版本：1.3.0
- Deployment result: NOT_RUN。P03 当前精确 Commit 的线上服务器构建与 staging 部署尚未完成。
- Health check result: NOT_RUN for P03；2026-08-08 部署前只读探针返回 HTTP 200，但公开 `/version` 仍为 `core-api / 1.2.0-7fd3ed6`，仅证明现有 P02 staging 健康，不是 P03 部署证据。
- Rollback result: NOT_RUN；没有 P03 部署就不存在可验证的 P03 回滚。
- Environment: `obx-test` 隔离 staging；部署前容器 `ylven-p01-api` 正在运行镜像 `ylven-p01-api:7fd3ed6`，仅发布 `127.0.0.1:28201`。
- Pre-deployment compose: `/opt/ylven-p01/p02-compose.yaml`，SHA-256 `21277f82f4ecbc8256b67e7a86554dbca8e58bd2dbf7e15896d797e55b397b55`；仓库 P03 Compose 已将 `APP_VERSION` 更新为 `1.3.0-${YLVEN_IMAGE_TAG}`，仍须从最终精确 Commit 构建、部署和回读后填写实际证据。
- Source snapshot before server build: GitHub ref `phase/p03-conversations-streaming-chat-and-messages`，发布前不得把该源码快照当作已部署服务。

## 政策变更前的历史诊断

- GitHub Actions run：`31208006168`，commit `af9b7bfbd97d3168122568ae520e57e41ecb2a34`。
- 后端、Web、合同、Android 编译、单测与 Lint：PASS。
- 当时的模拟器视觉门禁：FAIL。该 run 只保留为历史诊断；根据当前规则，其构建、截图和 diagnostics 均不得作为 P03 APK、真机或部署验收证据。

部署完成后必须补充精确 commit、线上服务器镜像与构建来源、下载 SHA-256、公开健康检查、聊天受控失败/成功路径、回滚演练及其时间戳。
