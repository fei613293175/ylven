# P03 部署证据

- 阶段：P03
- 版本：1.3.0
- Deployment result: SUCCESS
- Health check result: PASS
- Rollback result: NOT_APPLICABLE
- Rollback evidence: API 上一镜像和 Nginx vhost 部署前备份均保留；本次未执行会改变当前 staging 数据的回滚切换。

## API 与健康状态

- 公共版本回读：`{"service":"core-api","version":"1.3.0-d2f2624"}`。
- `https://ai-admin.orbexa.cc/healthz`：HTTP 200，`status=healthy`，配置检查 healthy。
- `https://ai-admin.orbexa.cc/readyz`：HTTP 200，`status=healthy`，配置检查 healthy。
- API 镜像中的 P03 生产代码在 `d2f2624` 之后未变化；后续候选提交只涉及 Android instrumentation、证据或 Nginx 路由。

## P03-034 路由修正

- 首次公共后台实测发现 `/internal/metrics/chat` 被 SPA fallback 返回为 HTML。
- Commit `920267d` 在 Nginx vhost 中增加精确代理 `location = /internal/metrics/chat`，不开放相邻 `/internal/metrics` 路径。
- 线上服务器 `nginx -t` 通过并平滑 reload；现行 vhost SHA-256 为 `24cbc30e7fef5b897447d32ca58ad910c5f49d658a5cb27c9618763a50ba5fff`。
- 公共复测：无授权返回 HTTP 403；授权管理员返回 HTTP 200 JSON 和 71 条真实聊天指标。

## Android 产物边界

- 正式 APK：`YLVEN-1.3.0-P03.apk`。
- 已验收生产 APK SHA-256：`48bd84d7c156ad5e3658e0372205359f751eddf0bf74fa024f34d6ae47158383`。
- 最终交付的精确 Commit、线上构建 run ID、源码归档 SHA-256 和本机下载 SHA-256 以发布目录中的 `服务器构建来源证明.json`、`本机下载校验证明.json` 和 `构建信息.json` 为准。
- 部署继续使用服务器已有受控 secret；未读取、复制或输出上游 API key 与管理员密码。

## 已知限制

- Redmi 最终定向 staging 尝试因该设备网络到 `ai-admin.orbexa.cc:443` 超时；相同生产 APK 已在 vivo 完成真实 staging 全流程，因此该记录归类为设备网络条件，不覆盖或伪造 Redmi 失败结果。
- ADR-010 与 ADR-011 已由项目所有者于 2026-08-11 批准；P03 不提前实现 P04/P06 功能。
