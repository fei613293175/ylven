# P03 所有者测试清单

- 阶段：P03
- 版本：1.3.0
- 当前结论：开发与 Codex 验收项 `PASS`；项目所有者最终结论仍由 `所有者验收.md` 单独记录。
- Feature 覆盖：P03-001..P03-035。

## 已执行的开发侧验证

- [x] 线上服务器 Go 测试通过，包含 P03 会话、SSE、取消、导出、反馈、语音审计、指标与诊断专项测试。
- [x] 线上服务器 Web Admin 测试和构建通过。
- [x] 合同：`scripts/07_VALIDATE_CONTRACTS.py --phase P03` 通过；P03 35 项 Feature、1,086 条声明测试。
- [x] 视觉合同门禁：`scripts/20_CHECK_UI_VISUAL_GATE.py --packet P03-W05` 通过 8 个页面。
- [x] 状态连续性：`scripts/38_VALIDATE_STATE_CONTINUITY.py` 通过。
- [x] 政策变更前的历史诊断 run `31208006168` 完成后端、Web、合同、Android 编译、单测与 Lint；其模拟器截图和 failed diagnostics 不属于当前发布证据，也不得用于 P03 验收结论。
- [x] 从冻结候选精确 Commit 在线上服务器构建 P03 APK 与 instrumentation APK，下载后复核服务器清单及每个文件 SHA-256；最终证明随正式发布目录更新。
- [x] 在物理真机上完成签名迁移安装、启动、真实 staging 流程、29 个 Interaction ID、93 个状态截图、逐页视觉比对和崩溃/ANR/日志审查。
- [x] 真实 P03 staging 部署与真实上游联调通过；完成态会话、导出、重命名、归档和临时会话删除有真机记录。
- [x] 公共管理后台真实管理员登录、会话/消息/run、71 条聊天指标、1,570 条审计事件和未授权 403 回读通过。

## 进入路径、操作步骤、正确预期

- [x] 从已复核 SHA-256 的线上服务器构建执行项目所有者批准的 P02 -> P03 一次性签名迁移：包名保持 `cc.orbexa.ylven`、版本为 1.3.0、证书匹配迁移合同，并记录旧本地数据与登录态不保留。
- [x] 冷启动后进入 AI 首页；P03-001..P03-003 的会话摘要、创建与分页来自当前用户数据。
- [x] 创建、重命名、归档、删除并搜索会话；P03-004..P03-007 状态更新及所有权边界通过。
- [x] 发送普通文本；P03-008..P03-015 创建真实 run、完成流式回答并持久化消息和元数据。
- [x] 生成中停止及断线恢复；P03-010..P03-013 的游标恢复、稳定失败与晚到回答抑制通过。
- [x] 前后台切换并恢复会话；P03-013、P03-016、P03-017 的运行状态与缓存协调通过。
- [x] Markdown 标题、代码块、表格和 URL 状态；P03-018..P03-021 的安全渲染、复制、横向滚动与引用通过。
- [x] 复制、单条导出、重新生成和反馈；P03-022..P03-027 的可见状态和审计通过。
- [x] 临时会话与完整导出；P03-028..P03-029 的历史隔离和 Markdown 内容通过。
- [x] 朗读、语音输入与草稿；P03-030..P03-032 的系统入口、审计和恢复通过。
- [x] 跨用户资源访问；P03-033 的会话、消息、run 和导出受控拒绝测试通过。
- [x] 管理员后台指标与会话诊断；P03-034..P03-035 的真实数据、完成 run、审计回读和未授权拒绝通过。

## 失败、恢复和已知限制

- 失败：记录 Feature ID、进入路径、操作步骤、实际结果、设备/系统版本、网络与截图；P0/P1 未关闭不得批准。
- 恢复：先保存失败现场，再以原始输入重试；对取消/断线场景记录 run ID 与事件游标，禁止用新会话掩盖问题。
- 已知限制：未配置 `SUB2API_ENDPOINT` 或 API key 时聊天运行应为受控 `503 chat_runtime_unavailable`；这不是验收通过。
- 硬门禁：本机无 Gradle不影响发布；Android 只在线上服务器构建。设备不可用时必须停止；多台真机在线时优先选择空闲设备，全部占用时进入最短共享 FIFO 等待；不得使用 GitHub Actions、Android Emulator 或任何模拟器回退。

验收人：__________  测试设备/浏览器：__________  测试日期：__________

最终结论：Codex 验收 `PASS`；项目所有者在正式发布目录的 `所有者验收.md` 记录 `APPROVED` 或 `REJECTED`。
