# P03 所有者测试清单

- 阶段：P03
- 版本：1.3.0
- 当前结论：`PENDING`。本清单只能在精确 Commit 的线上服务器 APK 下载并通过 SHA-256 复核后，于项目所有者本机已连接的物理 Android 手机上执行；未填完不应改为 `APPROVED`。
- Feature 覆盖：P03-001..P03-035。

## 已执行的开发侧验证

- [x] 本地 Go 1.26.0：`go test ./...` 通过，包含 P03 会话、SSE、取消、导出、反馈、语音审计、指标与诊断专项测试。
- [x] Web Admin：`npm test` 和 `npm run build` 通过。
- [x] 合同：`scripts/07_VALIDATE_CONTRACTS.py --phase P03` 通过；P03 35 项 Feature、1,086 条声明测试。
- [x] 视觉合同门禁：`scripts/20_CHECK_UI_VISUAL_GATE.py --packet P03-W05` 通过 8 个页面。
- [x] 状态连续性：`scripts/38_VALIDATE_STATE_CONTINUITY.py` 通过。
- [x] 政策变更前的历史诊断 run `31208006168` 完成后端、Web、合同、Android 编译、单测与 Lint；其模拟器截图和 failed diagnostics 不属于当前发布证据，也不得用于 P03 验收结论。
- [ ] 从当前精确 Commit 在线上服务器构建 P03 APK 与 instrumentation APK，下载后复核服务器清单及每个文件 SHA-256。
- [ ] 在物理真机上完成覆盖安装、启动、真实 staging 流程、29 个 Interaction ID、93 个状态截图、逐页视觉比对和崩溃/ANR/日志审查。
- [ ] 真实 P03 staging 部署与真实上游凭据联调：待执行。

## 进入路径、操作步骤、正确预期

- [ ] 从已复核 SHA-256 的线上服务器构建用 `adb install -r` 覆盖 P02 1.2.0；正确预期：安装成功、应用数据未被安装过程清除、版本为 1.3.0。
- [ ] 冷启动后进入 AI 首页；正确预期：P03-001..P03-003 的会话摘要、创建与分页均来自当前用户数据。
- [ ] 创建、重命名、归档、删除并搜索会话；正确预期：P03-004..P03-007 更新正确且不会显示其他用户会话。
- [ ] 发送一条普通文本；正确预期：P03-008..P03-015 创建 run，显示流式状态，完成后保留消息和元数据。
- [ ] 在生成中停止回答并断网/恢复；正确预期：P03-010..P03-013 可按游标恢复或显示稳定失败，不写入取消后的晚到回答。
- [ ] 前后台切换、杀进程再返回会话；正确预期：P03-013、P03-016、P03-017 的运行状态和缓存协调可恢复。
- [ ] 发送含标题、代码块、表格和 URL 的回答；正确预期：P03-018..P03-021 安全渲染、可复制代码、表格可横向滚动、引用可见。
- [ ] 使用复制、单条导出、重新生成和反馈；正确预期：P03-022..P03-027 操作可审计、状态可见、失败文本可回填重试。
- [ ] 创建临时会话并导出完整会话；正确预期：P03-028..P03-029 临时数据不污染历史，Markdown 内容完整。
- [ ] 对助手消息使用朗读与语音输入；正确预期：P03-030..P03-032 创建审计语音作业，转写文本仍可编辑，草稿可恢复。
- [ ] 用两个测试用户交叉访问会话、消息、run 和导出；正确预期：P03-033 返回受控拒绝，不泄露任何资源。
- [ ] 以管理员在后台查看指标和会话诊断；正确预期：P03-034..P03-035 读取真实数据、审计可回读、非管理员被拒绝。

## 失败、恢复和已知限制

- 失败：记录 Feature ID、进入路径、操作步骤、实际结果、设备/系统版本、网络与截图；P0/P1 未关闭不得批准。
- 恢复：先保存失败现场，再以原始输入重试；对取消/断线场景记录 run ID 与事件游标，禁止用新会话掩盖问题。
- 已知限制：未配置 `SUB2API_ENDPOINT` 或 API key 时聊天运行应为受控 `503 chat_runtime_unavailable`；这不是验收通过。
- 硬门禁：本机无 Gradle不影响发布；Android 只在线上服务器构建。设备不可用时必须停止；多台真机在线时优先选择空闲设备，全部占用时进入最短共享 FIFO 等待；不得使用 GitHub Actions、Android Emulator 或任何模拟器回退。

验收人：__________  测试设备/浏览器：__________  测试日期：__________

最终结论：`PENDING`（全部 P0/P1 关闭、线上服务器来源与下载哈希一致、真机功能/日志/视觉证据完整后，由项目所有者改为 `APPROVED`；否则为 `REJECTED`）。
