# P04 实施状态

- 阶段：P04 - 多模型、推理路由与回答对比
- 当前工作包：P04-W02
- 当前状态：DOING
- 最后更新：2026-08-17

## 已证实事实

- 唯一状态控制器已将 `P04-W01` 以 `DEFERRED_WITH_REASON` 最终化，并推进到 `P04-W02`；W01 未被报告为完全通过。
- W02 已修复消息运行接口的默认选择优先级：请求未传模型时，先解析会话和全局默认，再检查相应路由。
- W02 定向服务器回归已通过：`TestP04W02HTTPResolvesDefaultsBeforeRoutingAndPreservesRunProvenance`、`TestP04W02ExplicitOverrideWinsAndBranchSnapshotsOnlyHistoryBeforeFork`，以及既有的分支和重答行为回归。
- P04-W02 的物理设备验收脚本仅会执行 W02 专用 live flow 与 25 个 W02 Android 合同状态，不能将 W03-W05 的状态计入本工作包。
- staging 目前只有 `auto` 路径；未配置可授权的第二模型运行通道、路由策略或能力探测。因此，真实第二模型重答和非自动推理映射不能被测试夹具、静态状态板或目录选择器替代。

## 当前工作

- 在连接服务器上构建精确 W02 提交，并用真实 Android 设备完成安装、启动、页面点击/输入/返回/滚动、日志与 ANR 审查。
- 真实页面流程会覆盖全局默认、会话默认、临时模型选择、回答来源元数据和回答分支；仅已配置的默认模型可作为 staging 完成回答证据。
- 真实后台会话、第二模型路由和非自动档位仍需外部配置才可收敛为完整验收。

## 边界

P04-W01 的视觉例外只适用于 W01。它不能用于关闭 P04-W02 或后续工作包，也不能把外部配置缺失改写为功能通过。
